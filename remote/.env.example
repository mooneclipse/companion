# companion-remote 設定例。実際の値は .env にコピーして使う(.env は .gitignore 済)。
# systemd service(RA-6)が EnvironmentFile で読み込む想定。

# サーバの listen ポート(127.0.0.1 のみ。tailscale serve がこの port を前段で叩く)。
# dashboard nowplaying-helper(47823)と衝突しない値。
REMOTE_PORT=47824
