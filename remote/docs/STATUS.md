# companion-remote 開発台帳（フェーズ外単発ユーティリティ: スマホ専用リモコン PWA）

最終更新: 2026-07-14 (**#105 やることの完了ボタンを撤去 = 完了操作を AI 側 tickets.py done に一本化・commit 77168b1** = user 起票「編集と完了が近い、編集中に完了が押せてしまう」。編集/完了が gap 6px 隣接 + 完了だけ非可逆で確認なし・UI 復活手段なしの非対称が本質。4 案提示 → user 裁定で AI 移管 (完了経路 = 手元/Telegram #chat とも tickets.py done 許可済みを裏取り)。PWA から完了ボタン + doneTodo 削除・完了方法の note 追加、server から使い手ゼロの POST /api/todo/status 削除。SW v42→v43。153 tests pass + code-reviewer + restart 実施 + loopback (health 200 / 削除 route 404 / 既存 401 維持)。**Pixel 6 実機確認 2026-07-14 完了 (user「実機確認できた OK」)**。下記 Done 参照) / 2026-07-13 (**tickets CLI の --help 誤起票対策・commit 2b33f57** = 「add --help で --help が本文として誤起票される事故が反復」(直近 #88、user 指摘)。help/-h/--help はどの位置でも usage 表示のみ (state 非接触・rc=0)、`_parse_add` は --by 以外の -- 始まりトークンと --by 値欠落を reject、usage 抽出の `split("CLI",1)` 誤マッチの既存の粗も `_usage()` で修正。server API 面は不変 = restart 不要。153 tests pass、code-reviewer 修正必須ゼロ。下記 Done 参照) / 2026-07-10 (**#77 ytcheck フィードバックに URL 貼り付け記入を追加 (PWA のみ)** = user 起票「一覧からチェックを打つ方式だと 1 日数本しか見ないのに探すのが大変」。ytcheck 画面上部に「URL で記入」カード追加: YouTube URL を貼り付けて ○/× タップで checked+feedback を一括記入。サーバー無変更 (既存 /api/ytcheck/feedback をそのまま利用)。URL→video_id 抽出は youtu.be/watch/shorts/live/embed/裸 ID の 5 パターン対応 (11 文字固定)。当月の viewing に無ければ前月もフォールバック (月替わり直後対応)、2 ヶ月以上前や推薦外の動画は「推薦一覧にありません」、通信障害は「記入に失敗しました」と区別。SW v41→v42。検証 = node --check OK + URL パースユニットテスト 8/8 + code-reviewer 修正必須ゼロ・軽微 3 件反映 (var→const/let・登録順・netErr 分岐) + restart 実施 + loopback health 200。commit 002a6ea。**Pixel 6 実機確認 2026-07-10 完了 (user「確認できた OK」)**。下記 Done 参照) / 2026-07-10 (**#76 vault 一覧に全フォルダ横断の更新順モードを追加** = user 起票「ノート一覧に更新順モードがほしい」。server: `list_notes()` の各 note dict に `mtime` (int、`os.stat().st_mtime`) を追加。PWA: 検索欄下にフォルダ/更新順のトグルバー (localStorage 永続)。更新順モードは全フォルダの notes を mtime 降順のフラットリスト表示 (日時併記)、検索中はモード非適用 (検索結果はフラット維持)。SW v40→v41。検証 = 単体 144 件全 PASS (mtime 2 件追加) + node --check 構文 OK + code-reviewer 修正必須ゼロ・軽微 3 件 (docstring 反映済、他 2 件は任意見送り) + restart 実施 + loopback health 200 / vault/list 無トークン 401。commit d421837 + b121b09。**Pixel 6 実機確認 2026-07-10 完了 (user「確認できた OK」)**。下記 Done 参照) / 2026-07-08 (**#59 NowPlaying TV の TV 実運用確認 OK (user 報告) = #59 クローズ + 台帳反映漏れの後始末** = 実装自体は 2026-06-30 完了済み (commit 97f91c9、B案: music.youtube.com URL 再生で mpv は vid=no の音声のみ + Firefox kiosk が HDMI-1 に曲名/アーティスト/アルバムアート/プログレスバー/時計を全画面表示、停止 or 非 music URL で Firefox kill) + HDMI-1 配置 fix (ba242c0、2026-07-06) だが STATUS.md 未記載・チケット未クローズのまま残っていた。コード変更なし・restart 不要、詳細は下記 Done 参照) / 2026-07-08 (**#71 Pixel 6 実機確認完了 (user「確認できた OK」) = #71 一式クローズ**、詳細は 1 つ後ろの entry と下記 Done) / 2026-07-08 (**#71 実機フィードバック対応: 巡回チャンネル編集を独立画面化 + 操作結果の表示位置を手元に (PWA のみ)** = Pixel 6 実機 user フィードバック 2 件。①「登録 (追加) が無反応」の原因調査 = 機能自体は本番動作 (削除 3 件・更新 1 件が ytcheck repo に自動 commit 済みを git log で確認、add はログ・commit とも痕跡ゼロ)。真因は**エラー文言が一覧最上部の #yt-ch-msg に出て、53ch の一覧の下にある追加フォームから viewport 外** = @handle 形式の共有 URL (YouTube アプリの共有は https://youtube.com/@handle?si=… 形式) → 400 の文言が見えず無反応に見えた。対応 = 追加の結果/エラーをボタン直下 #yt-ch-add-msg に (成功時「追加しました」)、@を含む入力は送信前にその場で非対応と明示、**保存/削除/日数バリデーションもフォーム内 result に** (code-reviewer 指摘: add 側だけ直すと下方の行の編集で同じ障害モードが残存 = 次の実機報告で対症療法 2 周目になるため同 commit で閉じた)。②「フィードバック画面の一番下だと存在を忘れる」→ **#ytchannels 独立画面に分離** (SCREENS/openScreen/popstate の 3 点セット追加)、入口はフィードバック画面**上部**の data-open ボタン「巡回チャンネルの編集 ›」(initNav の既存列挙で結線、ホームタイルは増やさない)、戻る = history.back() でフィードバック画面に復帰。SW v39→v40。検証 = node --check + 旧 ID 残参照ゼロ + 単体 142 件 pass (server 無変更) + 隔離インスタンス相手の Playwright E2E 18/18 [遷移/一覧/フォーム内バリデーション文言/保存/@handle その場文言/追加/409/削除/戻る 2 段/pageerror 0] + スクリーンショット目視 + code-reviewer 修正必須ゼロ・軽微 1 件 (保存/削除の表示先) 反映。restart 不要 (web のみ)。commit 9509399。下記 Done 参照) / 2026-07-07 (**#71 巡回チャンネル編集 UI の接続 (#69 後段) 実装完了** = ytcheck 側 `channel_store.py` (flock + CRUD + git 自動 commit、ytcheck commit 2419110) を feedback_report と同型の sys.path append で import 流用し、①server: `server/ytcheck.py` に `parse_channel_id` [素の UC ID か youtube.com/channel/UC〜 URL の fullmatch のみ受理、**@handle 非対応 = yt-dlp 非依存の設計判断**] + 検証値域 (favorite 1-5 / check_days 1-30 = ytcheck config と同値域 / note 500 / name 200) + list/add/update/remove の薄いラッパー [`REMOTE_YTCHECK_STORE_DIR` / 既存 CHANNELS_JSON env で上書き可のパス束縛のみ。排他・atomic write・ytcheck repo への git 自動 commit は store 側の責務、書き込みロジックを二重実装しない] + app.py に GET /api/ytcheck/channels + POST /api/ytcheck/channel/{add,update,delete} [全 Bearer 必須、genre 有効値は JSON の genres キーから引く = state が正、重複 ValueError→409 / 不在 KeyError→404、channel_id は主キーで書き換え禁止]。②PWA: `#ytcheck` 画面内に折りたたみ「巡回チャンネル」[ジャンル別一覧 → 行タップで編集フォーム 1 件展開 (favorite/check_days/genre/note + 保存・confirm 付き削除)、追加フォーム (UC ID or /channel/ URL + 名前 + ジャンル、check_days/favorite は 4/3 デフォルト)、POST 成功 → 一覧再取得で全再描画、開いた時のみ GET = ポーリング非搭載、createElement のみ]。genres 未定義の genre 現値は「（未定義）」option で維持し保存時は genre をペイロードから省く (無警告移動と 400 の両方を回避)。SW v38→v39。検証 = 単体 130→142 件 pass [parse_channel_id 受理/拒否 + CRUD 委譲 12 件] + 隔離インスタンス [別ポート 47899 + 隔離 tokens + env ダミー JSON、本番非接触] 24/24 [401/400/404/409/CRUD/JSON 実体反映/既存 viewing 回帰] + Playwright E2E スモーク 15/15 [隔離実 API 相手、pageerror 0] + code-reviewer 修正必須ゼロ・軽微 3 件反映 [placeholder を https 付きに (parse は scheme 必須) / note maxLength=500 / 未定義 genre 保護] + 本番 restart 後 loopback [health 200 / 新 4 route 無トークン 401 = 404 でない / 偽 route 404、本番 JSON への write なし]。**編集可能フィールドの範囲はチケット指示の user 確認 (AskUserQuestion) が無応答だったため推奨案で独断** = favorite/check_days/note + genre 編集可、追加/削除あり、@handle 追加は非対応 (name 編集は対象外)。restart 実施済。commit d92241f。下記 Done 参照) / 2026-07-07 (**F-ytcheck 実装完了 (#65 案 B、server + PWA + ytcheck 側 flock)** = 月次集計レポートのタイル表示 + `viewing-YYYY-MM.md` への視聴チェック/○× 記入 API。①server: `server/ytcheck.py` 新設 [ytcheck の `tools/feedback_report.py` (stdlib のみ) を sys.path append で import 流用・パーサ二重実装なし、GET は read-only、POST は **remote 初の外部 write** = 検証済み month からのみ組み立てた viewing ファイル 1 点に閉じる] + app.py に GET /api/ytcheck/viewing?month= / POST /api/ytcheck/feedback [両方 Bearer 必須、month は `\d{4}-\d{2}` fullmatch = traversal 防止、feedback は ○/×/空のみ、video_id は re.escape 照合、該当なし 404]。②**cross-process ロック (R3 消化)**: viewing ディレクトリ直下の共有 `.viewing.lock` を remote (set_feedback) と ytcheck (`update_viewing_history`、ytcheck repo 側 commit) の両プロセスが flock(LOCK_EX)。blocking・リトライ分岐なし。③PWA: タイル ✓ 10 YT巡回 + `#ytcheck` 画面 [月ナビ + レポート折りたたみ pre + チャンネル別一覧、チェック/○× タップ → POST → 返却 entry で再描画、createElement のみ]、SW v37→v38。検証 = 単体 108→130 件 pass [test_ytcheck 22 件 = month 検証/行書き換え/regex escape/ロック保持中ブロック] + ytcheck 側 229 件 pass + 隔離インスタンス [別ポート 47899 + 隔離 tokens + env ダミー viewing、本番非接触] 17/17 [authed GET 200 / POST 実書き換え / 401 / 400 / 404] + Playwright UI スモーク 19/19 [pageerror 0] + 本番 restart 後 loopback [health 200 / 新 route 無トークン 401 = 404 でない / 既存 route 401 維持、本番への write なし]。commit は下記 Done 参照) / 2026-07-07 (**コピーボタンのコピー形式を `#N` 単独に変更 (PWA のみ)** = user 裁定「補足もあるから番号だけがいい」。本文は貼り付け先の claude セッションが `tickets.py show N` で実物を引くため冗長。素の `#N` がコンテキスト空のセッションでも通じるよう **workspace/CLAUDE.md に「チケット番号 (#N) の受け方」を新設** (常時読み込み側で保証) + memory `project_companion_shared_todo` も同期。SW v36→v37。検証 = Playwright 8 アサーション全 PASS (clipboard 実読み "#12"/"#13") + code-reviewer 修正必須ゼロ。restart 不要。commit 11de0a4。下記 Done 参照) / 2026-07-07 (**やること画面のボタン列を 2 行目へ落として右寄せ (CSS のみ)** = コピーボタン追加直後の Pixel 6 実機 user フィードバック「3 つのボタンが本文を圧迫してる」対応。`.todo-item` flex-wrap:wrap + `.todo-actions` flex:0 0 100% / justify-content:flex-end で本文が 1 行目全幅 (412px で 160px 程度→308px)、ボタンは 2 行目右寄せ。dl-item はボタン li 直付け (.todo-actions 不使用) で無影響、編集モードも input 全幅 + 保存/戻す 下段 (code-reviewer が実コード裏取り、修正必須ゼロ・軽微 1 件 flex shorthand 統合を反映)。SW v35→v36。検証 = Playwright 412x915 で 7 アサーション全 PASS + スクリーンショット目視。restart 不要。commit ac0167c。下記 Done 参照) / 2026-07-07 (**やること画面にチケットのコピーボタン追加 (PWA のみ)** = user 依頼「ターミナルに切り替えてる間に忘れるからコピーボタンがほしい」。各チケット行を コピー/編集/完了 の 3 ボタン化、コピーは `#N 本文` 形式で navigator.clipboard.writeText (ターミナルの claude セッションへ番号ごと貼り「#N やって」と渡す用途、tailscale serve HTTPS = secure context 前提)。成功「済」/失敗「失敗」を 1.2s ボタン文言表示 (リトライ・エラー分類なし)。createElement のみ・CSS 無変更 (.todo-done 流用)。SW v34→v35。検証 = node --check + Playwright + Chromium (web/ 静的配信 + /api 全モック、本番非接触) 8 アサーション全 PASS (clipboard 実読み・文言遷移・編集/完了回帰・pageerror 0) + code-reviewer 修正必須ゼロ。restart 不要 (web のみ)。commit 75aa755。下記 Done 参照) / 2026-07-07 (**#65 ytcheck フィードバック連携: 調査 + スコープ確定 = 台帳・チケットのみ、実装なし** = user 起票「ytcheck のフィードバックと巡回チャンネル編集をリモコンから」を調査 (Explore subagent 2 本 + 一次確認)。3 機能に分解し **案 B (集計閲覧 + ○/× 記入) を先行、チャンネル編集は #69 に分離** を user 裁定。設計論点 = remote 初の外部 write (vault/notes/ytcheck/ の viewing ファイル、R3 の cross-process ロック要) / ytcheck 側 youtube-channels.json はロックなしで毎朝 05:00 に書き戻しあり → 編集は ytcheck 側 flock+CRUD 先行の 2 段構え。詳細は下記 TODO「F-ytcheck」参照) / 2026-07-07 (**F-vault ビューア可読性改修 + 表示されない画像の修正 (PWA のみ)** = user 依頼「ノートをもっと読みやすく + まだ表示されない画像がある」。原因調査 = 表示されない画像は ①index.html の CSP img-src が pbs.twimg.com 限定のため st-note / publish.obsidian.md / 絵文字 SVG 等の外部画像 約17件が全ブロック ②YouTube/x.com の動画 URL を `![](…)` した 8 件が img 化され恒久壊れ画像 (server 側の画像 API / attachments 経路は健全)。画像修正 = **A-1** CSP img-src を `'self' blob: https:` に拡大 [script/style 等の他ディレクティブは 'self' 維持] / **A-2** 外部 img の error 時にホスト名入り「▶ <host> で開く」リンクへ createElement 置換 [http(s) スキーム限定・blob 画像 (wikilink 由来) は対象外] + .vault-img-missing / .vault-img-fallback CSS 新設。可読性改修 = **B-1** marked breaks:true [Obsidian と同じ 1 改行=改行表示] / **B-2** タスク checkbox 復活 [vault 全体 1117 箇所が対象。DOMPurify FORBID_TAGS から input のみ除外 + 描画後に type=checkbox 以外 remove・disabled 強制・li に task-list-item 付与 (vendored marked は class を付けないと node 実測)] / **B-3** 本文ヘッダ 2 段化 [フォルダ小 + ノート名大、#vault-doc-path 追加] / **B-4** 一覧のフォルダ折りたたみ [details/summary、既定全閉、開閉状態をモジュールレベル Set で保持、検索結果はフラット維持] / **B-5** 検索ヒット語の mark ハイライト [indexOf 分割 + createElement/textNode 構築、innerHTML 不使用]。SW v33→v34。**検証 = implementer subagent が Playwright + Chromium 実ブラウザ 24 アサーション全 PASS (securitypolicyviolation 監視で CSP violation ゼロ含む) + スクリーンショット目視 + 既存単体 108 件 pass (server 非変更) + code-reviewer 修正必須ゼロ・軽微 1 件 (フォールバック href の非 http(s) スキーム除去) 反映済**。innerHTML は DOMPurify 通過後の 1 箇所規律を維持。restart 不要 (web のみ、SW bump で PWA へ到達)。commit 5604ed8。下記 Done 参照) / 2026-07-03 (**RV-8 クローズを台帳反映** = 2026-06-29 のチケット棚卸しセッションで決定済みだった記録の反映漏れ後始末。高 bitrate Live × Wi-Fi 2.4GHz 帯域上限が根本原因 (mpv buffer 調整は的外れ、対策は 5GHz/有線化/480p 受容 = ユーザー環境変更) につきクローズ、再現時に再起票。TODO ブロック 26 行を 1 行クローズ記録に圧縮、切り分け経緯は git 履歴参照。コード変更なし・restart 不要) / 2026-06-20 (**思考ログタイル追加 (server + PWA)** = bot の `companion_thoughts.jsonl` ({timestamp, observation} 追記式) を PWA から read-only 時系列で「そっと覗く」タイルを 1 枚追加 (user 要望)。**軸4拡張 機構1 の思想境界を表示側でも厳守** (思考ログ = 読まれない前提で書く / 不在の核) = ①プッシュ・通知・新着バッジ・未読件数ゼロ ②read-only 厳守 (編集・削除・既読・お気に入りなし) ③サーバ無加工 (観察行をそのまま透過、解釈・要約・感情ラベルなし、timestamp の日時化のみ可で本文不可触)。①server: `server/thoughts.py` 新設 [JSONL を 1 行ずつ json.loads・壊れ行/空行 skip・最新が上で reverse・MAX_ENTRIES=200 cap・ファイル不在/空/全行壊れは graceful に空配列・open は "r" のみ・固定パス読みで REMOTE_THOUGHTS_PATH 上書き可] + app.py に `import thoughts` + `api_thoughts` + ROUTES `("GET","/api/thoughts")` [Bearer 必須・書き込み endpoint なし]。②PWA: ホーム grid にタイル [… 07、新着バッジ DOM なし] + tile-count 6→7、`#thoughts` 縦ライン timeline [createElement のみ・timestamp 従属 (fmtThoughtTime が epoch 秒/ミリ/ISO を best-effort 整形・解釈不能は原文透過)・observation 主役 (pre-wrap・textContent)・空は「まだ何もありません」・開いた時/popstate で取得、15s ポーリング非搭載]、SW v25→v26。**検証 = 既存単体 100 件 pass (回帰なし) + node --check 構文 OK + 本番 restart 後 loopback curl (/api/health 200 / /api/thoughts 無トークン 401 = route+auth・404 でない / 既存 7 route 401 維持 + 偽 route 404 = 回帰なし) + 隔離インスタンス (別ポート + 隔離 tokens + REMOTE_THOUGHTS_PATH ダミー、本番 .state/本番 thoughts 非接触) で authed 200 = 3 件最新が上・壊れ行 skip・epoch/ISO 混在透過・誤トークン 401・ファイル削除で空配列 を実観測。検証ダミー JSONL は確認後削除、bot/ 側書き込み経路は一切非接触。** restart 実施済。push 不要 ((C) ローカル git のみ)。下記 Done 参照) / 2026-06-18 (**RV-12 動画プレイリスト対応 (server + PWA)** = YouTube/ニコニコ等の list= プレイリストを mpv ネイティブ内部 playlist で連続再生。①server: `server/playlist.py` 新設 [yt-dlp --flat-playlist -J で (url,title) 展開、dlqueue.py 同型の env 隔離/killpg/timeout/-4、C3 先頭 100 件 cap・C4 allowlist 外 skip・rc+JSON 1 回確定] + video.py に play_playlist_load/queue_next/prev/jump [固定テンプレートのみ、外部文字列を property に流さない=RCE 一線、投入後 playlist 非編集=C1] + _STATE_PROPS/_derive に playlist-pos/count + app.py に /api/video/play_playlist・queue/{next,prev,jump} [全 Bearer]。②PWA: 再生ボタンが list= 検出で play_playlist へ自動振り分け [URL 欄 1 本維持]、#video-queue 一覧+現在曲ハイライト+前/次+曲タップ jump、title は PWA 保持 [vQueueTitles=メモリ+localStorage、契約 C2、server stateless 維持]、vExpanding で展開中 [最大 60s、mpv idle] の「読み込み失敗」誤判定を抑止、SW v24→v25。**検証 = 単体 110 件 pass [test_playlist 10 件] + headless mpv 0.34.1 [別 socket、本番非接触] で append/pos/next/prev/jump/C1/stop の実挙動全 PASS + Playwright UI スモーク 14 アサーション [web/ 使い捨て配信 + /api 全モック、本番サーバ/TV/token 非接触] pass。code-reviewer 修正必須 = server 1 件 [app.py の import playlist 欠落] + PWA 2 件 [videoStop の vExpanding 未解除 60s 固着 / innerHTML 方針違反] 反映。** server (8b40e87) + PWA (a9a6666) + docs (769a877/workspace 09886b2)。**TV 実機検証 2026-06-18 完了** = 5 曲投入→次へ→自動 advance(eof)→前へ→jump→PWA 閉じて継続→再開 pos 復元 を本番 TV で確認 (全て pause=false 再生継続)。**実機で append エントリの pause バグを発見・修正** = per-file `pause=no` の無いエントリは実 vo で current になると paused 読み込み → 自動 advance/次へが各曲で止まる中核破綻。play_playlist_load の append にも `pause=no` を付けて解決 (headless vo=null では再現せず、本番 TV で確定。reactive な後付け pause は自動 advance を救えず不採用)。設計大元 = `workspace/redesign/video-design.md` §0 punt 解除。下記 Done 参照) / 2026-06-14 (**F-2 発話を撤去 + ホームに「写真」タイル追加** = user 依頼「発話はもう使わない、削除アンド追加」。発話 UI/server/voice.py を一式削除[say.sh 本体は無改変・voice 通知/renbot は別軌道]、未使用化した threading/time import も除去、dlqueue.py の voice.py 同型コメントを更新。写真は `data-href` ランチャタイル[▦ 02]で別ポートの独立フォト PWA[tailscale serve :8446 → 127.0.0.1:47826、`~/companion/photos`]を別タブで開く[remote は写真を proxy しない]。SW v23→v24。単体 pass + Playwright スモーク[写真タイル click で 8446 へ window.open / 発話導線消失 / 動画・todo・vault・games 回帰なし] + restart 実施。下記 Done 参照) / 2026-06-12 (**RV-11 TVer 対応完了 + 動画画面整理** = user 依頼 2 件。①TVer 再生対応: 手順どおり検証が先 = TVer platform API から実エピソード 3 本 [3 系列局] を取得し yt-dlp 2026.03.17 `-J` で解決検証 [**DRM 0 / HLS m3u8_native / 無ログイン**] → urlguard ALLOWED_HOSTS に tver.jp / www.tver.jp 追加 + canonical ベクタ受理 2・拒否 2 追加 [全 90 件 pass]、bot.py ミラー同期 [47 件 pass]。**事前 DL は不可と裁定** = 期限付き見逃し配信のローカル保存は期限・権利の両面を迂回 → `normalize_dl` (STREAM_ONLY_HOSTS 除外) を新設し /api/dl の門を分離。本番実弾 = TVer 投入 11s playing / シーク ±60s / 停止、DL 経路 400 拒否を loopback 実観測。②動画画面整理: URL 欄を再生/事前DL で 1 本化 [入力欄 2 つ→1 つ、「あとでDL」ボタン化]、実態に合わない文言を更新 [placeholder 3 サービス / 400 文言 / resolve 目安 40〜70s→実測 10 秒前後]、軽量仕様を **`docs/video-ui.md`** に新設。SW v22→v23。Playwright UI スモーク 10 件 pass + JS エラーなし。restart 実施済。下記 Done 参照) / 2026-06-12 (RV-10 実運用初日の DL 失敗を修正 + **RV-9 ニコニコストリーミング解決** + bot /play 切替支援 = user 依頼 3 件。①DL 失敗 (timeout 3600s) の真因 = **網の IPv6 100% loss × CDN AAAA 応答で fragment 毎に IPv6 connect timeout ~40s** → dlqueue.py に `-4` 追加で 2.5KB/s→3.35MiB/s (90 倍)、失敗していた実 URL 258MB が 99s 完走。**ログイン (cookies) は速度に無関係を実測確定** (cookies あり/なし同速) → ticket #18 の --cookies-from-browser は不採用 (根拠 = dlqueue-design v1.2 §2.2)。②RV-9 = upstream **v0.37.0 ytdl_hook.lua を無改変 vendored** (`vendor/ytdl_hook.lua`) し `--ytdl=no` + `--scripts=` で 0.34.1 core に載せる + raw-options に `force-ipv4=` (§8 の 1 回の設計判断、撤去条件 = mpv 0.37+ 更新時、video-design §3.3/§8/改版履歴)。実機 = ニコニコ sm9 8s playing / YouTube VOD 回帰なし / 本番 TV 10s playing / 3 分 soak stall 0/38・cache 430s・underrun なし。mpv unit restart 実施済。③bot /play 切替 (ticket #17) は bot repo 側 (bot/docs/STATUS.md 2026-06-12)。known-good ペア更新 = mpv 0.34.1 core + vendored hook v0.37.0 + yt-dlp 2026.03.17。下記 Done 参照) / 2026-06-12 (RV-10 事前ダウンロードキュー実装完了 = F-dl。「RV-10 やって」名指し着手、設計ラウンド 1 回[`workspace/redesign/dlqueue-design.md` v1.1 = devil subagent 指摘 17 件反映]→ server/dlqueue.py[flock state + atomic save + 単一 worker thread + yt-dlp 直叩き env 隔離 + 容量 20GiB 二重ガード + Telegram 通知 bot.py 無改変] + /api/dl 3 ルート + /api/video/play_local[HTTP から path 非受理・int id のみ、realpath/commonpath 境界] + PWA 動画画面内折りたたみ「事前DL」+ SW v21→v22。単体 28 件追加で全 88 件 pass、loopback 隔離実観測 11 項目 pass、実弾 = YouTube 短尺 47s + **ニコニコ sm9 を yt-dlp 直 DL 1135s で成功 → RV-9 の domand cookies 403 を DL 経路が迂回することを実証**、headless mpv[vo/ao null、TV 非接触]で play_local → playing 到達 2 本とも確認。restart 実施済。RV-9 要否は user 再判定待ち。下記 Done 参照) / 2026-06-12 (動画まわり拡張ロードマップを TODO 化 = RV-10 事前ダウンロードキュー[外出先投入→帰宅後ローカル即再生、設計ラウンド 1 回付き、完了後に RV-9 要否再判定]・RV-11 TVer 実 URL 検証[検証が先、allowlist 追加はその後]を user 確定順で追加。実装なし、台帳のみ) / 2026-06-11 (ニコニコ動画対応 = URL allowlist 拡張。urlguard ALLOWED_HOSTS に nicovideo.jp / www.nicovideo.jp / sp.nicovideo.jp / nico.ms の 4 host 追加[normalize ロジック無改変、embed.nicovideo.jp はユーザーが貼る共有 URL でないため除外、live.nicovideo.jp=ニコ生はスコープ外で将来候補]。canonical ベクタ[video-design §4.1]に受理 4 + 拒否 4[userinfo 詐称/suffix 偽装×2/embed]追加で全 60 件 pass、bot.py ミラーも同期[bot repo commit]。known-good: yt-dlp 2026.03.17 で sm9 -J 57s / nico.ms 42s[orc 実測]。**mpv 実再生スモークは失敗(2 回再現)** = resolving 40〜45s→idle、真因は domand 配信が要求する per-format cookies フィールド[domand_bid]を mpv 0.34.1 ytdl_hook が未対応→HLS 403[cookie なし GET=403/あり=200 実証]。対症療法は打たず RV-9 に分離・設計判断待ち。restart 実施済[companion-remote のみ、mpv unit 無改変]。下記 Done 参照) / 2026-06-09 (やること画面に完了履歴の折りたたみ一覧を追加 = ticket #3。done チケットを後で振り返れる閲覧専用 UI。tickets.history()[done のみを updated 降順=新しい完了が先頭で返す、active()/all_tickets() の _locked()+_load() パターンに揃える] + GET /api/todo/history[Bearer 必須、閲覧専用ゆえ done→todo 復活/編集 endpoint なし=YAGNI]。PWA は別画面・別タイルでなくやること画面内に折りたたみ同居[履歴は主要動線でないため]、開いた時/todo 画面表示時のみ取得[active 一覧の 15s ポーリングには乗せない=履歴は頻繁に変わらず無駄ポーリングを増やさない]。各項目に完了日時[updated epoch→ローカル YYYY-MM-DD HH:MM、整形は fmtDateTime ヘルパ]表示、done は grey 系でトーンダウンし active と区別。createElement のみ[innerHTML 不使用]継続。test_tickets.py に history() 検証 3 件追加[done のみ/updated 降順/done 0 件で空]、全 57+3=60 件 pass。loopback HTTP 往復は隔離 temp state 上で実観測[401 無トークン / 200 done のみ updated 降順 / active が done を含まない]、本番 .state/tickets.json は非接触。SW CACHE remote-v18→v19。restart 未実施[反映には systemctl --user restart companion-remote.service]。下記 Done 参照) / 2026-06-08 (バージョン表記を追加 = 起動時に1回 git short hash + コミット日を確定する server/version.py + GET /api/version[Bearer 必須] + ホーム masthead の .ver 表示。ビルド工程なし[vendored 資産 + stdlib]のため版は HEAD から起動時1回読み、失敗は "unknown" フォールバック[リトライ/stderr 分岐なし=設計上限ルール準拠]。版は /api/health[無認証で情報を絞る方針]でなく authed 側に配置。文字列生成は純関数 format_version に切り出し test_version.py で 5 件検証[既存 52+5=57 全 PASS]。SW CACHE remote-v16→v17。restart 未実施[反映には systemctl --user restart companion-remote.service]。下記 Done 参照) / 2026-06-08 (F-vault ノート内画像の表示を追加 = 画像配信エンドポイント + CSP 緩和 + ローカル画像 blob 表示。原因 2 件[CSP が img-src 未指定で default-src 'self' フォールバック → clips のリモート画像 pbs.twimg.com 全滅 / サーバに画像配信経路なくローカル画像取得不可]を修正。server/vault.py に get_image[_safe_abspath の realpath+commonpath 境界判定を _resolve_in_vault に共通化、拡張子 allowlist のみ画像版に差し替え、read 専用 rb] + app.py GET /api/vault/image[Bearer 必須、403/404 写像は api_vault_get 一致、バイナリ送出は _Binary 1 型判定で icon png と同じ _send 再利用] / app.js は ![[name.ext]] → <img data-vault-img> 前処理 + DOMPurify ADD_ATTR + Bearer api() で blob fetch→object URL[src は Auth 送れないため必ず blob、再描画ごと revoke、失敗は握り潰し] / index.html CSP を img-src 'self' data: blob: https://pbs.twimg.com に緩和[inline 不許可・object-src/base-uri 維持] / SW は /api/ 既存 network-only でキャッシュ素通し、CACHE v14→v15。test_vault.py に get_image パス検証 10 件追加、全 52 件 pass。restart 未実施[反映には systemctl --user restart companion-remote.service]。下記 Done 参照) / 2026-06-03 (vault 閲覧機能 = F-vault read-only 追加。server/vault.py[realpath+commonpath traversal ガード / read 専用 / list・get・search 3 API Bearer 必須] + PWA「ノート」タイル/詳細[marked+DOMPurify クライアント描画 / wikilink ジャンプ / frontmatter 表示] + vendored marked@12.0.2・dompurify@3.0.11 + SW v12→v13、単体 42 全 pass[vault 19 件 = traversal 拒否含む] + loopback curl 往復 実観測 PASS。search は stdlib で安価ゆえ採用。下記 Done 参照) / 2026-06-03 (リモコン UI をホーム型タイルランチャー + ミニマルデザイン[紺地/朱赤]に全面リデザイン = ticket #2 完了。構造3案[下タブ/ホーム/集約]→ホーム型選定、見た目を並列デザイナー subagent 4案[温かみ/ネオン/ミニマル/家電]で発散→ミニマル選定、配色は紺地+アクセント朱赤 #d8392a 確定。選定は使い捨て mock を tailscale serve 経由で Pixel-6 実機プレビューさせて実施。app.js の状態機械/API/localStorage は全面流用し UI 構造のみ刷新、機能欠落なし、CSP/innerHTML 不使用継続、SW remote-v11→v12、Playwright 36 アサーション PASS、commit 178a9d9。下記 Done 参照) / 2026-06-03 (共用 TODO/inbox = F-todo v1-α 追加 [bot.py 非依存]。tickets.py + .state/tickets.lock flock / API 3 ルート Bearer 必須 / PWA 折りたたみカード + 件数バッジ / SW v10→v11 bump、単体 23 全 pass + loopback curl 往復 実観測 PASS。下記 Done 参照) / 2026-06-02 (第 3 作なごりリンク追加 = GAMES に :8444/nagori/ を 1 行、SW cache v8→v9 bump。下記 Done 参照) / 2026-06-02 (第 2 作ともしびリンク追加 = GAMES に :8444/tomoshibi/ を 1 行、SW cache v7→v8 bump。下記 Done 参照) / 2026-06-02 (ゲームリンク追加 = #app 最下部に折りたたみ「ゲーム」カード、GAMES 配列駆動でみちゆき:8444、SW cache v6→v7、Chromium 検証 PASS。下記 Done 参照) / 2026-06-01 (v1-β = F-1 bot.py 双方向化 RB-1/2/3 の位置づけを更新 [companion 全体 Phase 3→4 転換 + 同日追加判断: 条件 #2 を Phase 4 着手の門から外したため、凍結ではなく Phase 4 着手後の bot.py 大改変として様子見付きで入れる順序の後ろ倒し、PROJECT.md 健全性履歴 2026-06-01 entry]。F-2 voice / F-3 OS status [v1-α] は影響なし) / 2026-05-28 21:20 (RV-8 baseline 計測完了 = Wi-Fi 2.4GHz / ping IPv6 100% loss / IPv4 jitter 0.66ms。別 Live 1.07 Mbps では cache 10 秒先読み余裕 + underrun false でカクつき出ず、5 秒周期 stall は配信 bitrate 依存の新仮説。buffer 拡大は的外れの可能性高、修正は次回カクつく Live 再現時の IPC sampling 後に判断)

## 概要

スマホ (Pixel-6、Tailnet 同居) から Linux Mint 機 (miho-inspiron-3521) を「専用リモコン」として叩くための PWA + 配信サーバ。Discord アプリのアカウント切替制約を回避し、companion 機専用 UI で素早く操作することが目的。**対話完結型**(claude への指示も応答もリモコン UI で完結、Discord と行き来しない)。

- 用途: 専用リモコン (素早い open → 操作 → close)
- 対象ユーザ: miho 個人のみ (完全個人利用、家族端末・外部公開なし)
- ネットワーク境界: Tailscale 内のみ (Tailnet 外には絶対に出さない)
- 不要なもの: push 通知 / スマホハード機能 / マルチユーザ / 外部配布

## 位置付け

PROJECT.md の Phase 1〜4 ロードマップとは独立した **フェーズ外の単発ユーティリティ** (dashboard と同じ扱い)。

- **bot.py 改変方針 (設計で更新)**: 対話完結型(F-1 の claude 応答を remote UI に返す)を実現するため、当初の「bot.py 不可触」を緩め、**案B = bot.py 最小双方向化**(2本目 socket + request/response ハンドラ + 定数の最小差分、既存 notify/on_message/sessions 無改変)を採用。ただし bot.py を触るのは **F-1 のみ・v1-β(6/2後)** に限定。F-2/F-3 は bot.py 非依存。
- **Phase 2.5 健全性 2 週間観察 (2026-05-19 〜 2026-06-02) を汚さない**: bot.service restart を伴う F-1(v1-β)は観察窓後。F-2/F-3(v1-α)は bot.service 非依存で先行実装可。
- voice/ 側 bot/ 実装着手 (2026-06 上旬目処) とも独立。

## 設計

**確定設計**: `~/companion/workspace/redesign/remote-design.md` **v1.0**(2026-05-20 確定)。agent team `companion-remote-design`(architect/ux/security/devil + lead)の Round 1〜3 議論 + lead 裁定13件 + devil 最終確認(approve 可・致命級ゼロ)を経て確定。

確定の骨子:
- 責務境界 = 案B。振り分けは「claude を叩くか」の静的1ビットで設計時に1回確定(whack-a-mole 防止)。F-1=bot socket 経由、F-2/F-3=remote 直接。
- サーバ = stdlib http.server(ThreadingHTTPServer)+ ガードレール5点(RAM 3.7Gi 制約 / dashboard precedent / tailscale serve 前段)。FastAPI は不採用。
- 認証 = localStorage 永続トークン(SSH 発行→paste)+ Bearer + identity header 二段化(可能なら)。
- 実装 = ハイブリッド(v1-α 〜6/2 先行 / v1-β 6/2後)。

architect plan 全文は `workspace/redesign/remote-arch-plan-v2.md`(議論アーカイブ)。

## ディレクトリ構成（確定）

```
remote/
├── docs/STATUS.md, SETUP.md
├── server/   app.py(http.server) / auth.py / status.py / urlguard.py / video.py / dlqueue.py / tickets.py / vault.py / version.py
├── tests/    test_urlguard.py(§4.1 canonical ベクタ mirror + video._derive)
├── web/      index.html / app.js / style.css / manifest.json / sw.js / icons/
├── systemd/  companion-remote.service / companion-video-mpv.service(F-video 常駐 mpv)
├── .env.example / .gitignore
└── .state/   tokens.json(0o600) / jobs/(v1-β)
```

git の扱い: 2026-07-11 から **`~/companion/` モノレポ配下** (チケット #82、旧 (C) ローカル git は分割時代履歴ごと取り込み済み)。push されれば GitHub (mooneclipse/companion) にバックアップされるため、旧「(B) 昇格」判断 (RA-7) は不要になった。運用の正は `~/companion/CLAUDE.md` git 運用方針。

## TODO

### 実装着手前 user 確認4件 → 2026-05-20 全件裁定済（design §10）
- [x] ACL allow-all 撤去 → **据え置き**(撤去せず。境界は loopback bind + token。port 単位 ACL 化は後日の独立タスク)
- [x] ufw 化 → **見送り**(本タスクスコープ外。loopback bind + ACL で足り、自己ロックアウトリスク回避)
- [x] M-14 改訂文面 → **OK**(認可趣旨ベース改訂を承認。RA-7 で PROJECT.md L373 差替え)
- [x] H-1 認可退行 → **受容**(単一ユーザ・宅内・tailnet 限定到達、budget guard backstop で課金被害は月次 cap 頭打ち)

### v1-α サブタスク（〜6/2, bot.service 非依存、1サブタスク=1commit）→ 2026-05-20 完了
- [x] RA-1: scaffold + server skeleton(http.server 127.0.0.1, /api/health, ガードレール土台, .env.example, .gitignore)
- [x] RA-2: トークン認証(auth.py, token_urlsafe 発行 CLI, tokens.json 0o600 SHA-256 保存, Bearer compare_digest, 401, Content-Length cap + chunked 拒否)
- [x] RA-3: F-3 OS status のみ /api/status(df/free/sensors/uptime 直叩き。ledger/session 由来は含めない)
- [x] RA-4: F-2 voice POST /api/say(say.sh argv 直叩き + env 隔離実証 + speaker 整数 validation + rate-limit + exit code→HTTP 写像)
- [x] RA-5: PWA(index/app.js/manifest/sw[shell-only precache, API network-only]/icons, F-2 + F-3 OS + token paste, glance=接続+OS health, CSP + innerHTML 不使用)
- [x] RA-6: systemd/companion-remote.service + SETUP.md(tailscale serve / トークン発行 / SSH revoke / §9 serve 実機検証チェックリスト)
- [x] RA-7: docs/STATUS.md 反映 + PROJECT.md L373 M-14 文面改訂(R1, user 承認済) + N-3(v1.0 改稿で充足済) + (B)GitHub 昇格判断

### F-video サブタスク（v1-α 系列、bot.service 非依存、1サブタスク=1commit）→ 2026-05-20 実装完了
設計: `~/companion/workspace/redesign/video-design.md` v1.0。再生 state は mpv 所有、PWA は `GET /api/video/state` ポーリング(ジョブ基盤不要)。
- [x] RV-1: `systemd/companion-video-mpv.service`(Type=simple, --idle=yes, Environment 隔離 PATH=/home/miho/bin, --no-config, --ytdl-raw-options=ignore-config=,no-playlist=, ytdl_path 固定, --ytdl-format 720p上限, --fs-screen=0 暫定, --input-ipc-server + UMask=0077 で 0o600, ExecStartPre stale rm)
- [x] RV-2: `server/urlguard.py`(bot.py `_normalize_play_url` ミラー + §4.1 canonical ベクタ) + `server/video.py`(mpv IPC クライアント, verb whitelist 固定テンプレート json.dumps, 1接続1確定, phase 導出) + `tests/test_urlguard.py`(canonical ベクタ + _derive 計 8 テスト pass)
- [x] RV-3: `/api/video/*` ルート追加(app.py, Bearer 必須, play/pause/resume/stop/seek/volume/state, urlguard normalize→loadfile replace, 接続不能 503 / mpv エラー 502 / 構造化応答のみ stderr パースなし)
- [x] RV-4: PWA 動画パネル(状態機械 IDLE→RESOLVING→PLAYING⇄PAUSED + LIVE モード●LIVE + RESOLVING 経過秒/目安/キャンセル/90s 文言 + close≠stop + glance now-playing 統合 + 取りこぼし中立明示)。sw.js CACHE remote-v1→v2 bump。innerHTML 不使用継続
- [x] RV-5: docs/STATUS.md + SETUP.md(mpv unit 手順 + §10 build 検証 + churn 手順)反映 + known-good ペア台帳記録 + bot `/play` 判断
- [x] **build 検証(GUI/実機、SETUP §6) 🔴必須項目 完了 (2026-05-28)**: V-A1 ✅(2026-05-20) / V-A3 VOD ✅(2026-05-20) + Live hwdec ✅(2026-05-28 hwdec=vaapi 投入後 estimated-vf-fps 27.3→60.0 / %CPU 66.7→20.0 / decoder-frame-drop=0、IPC `hwdec-current=vaapi` 確認、user TV 物理確認で平均 fps 上は解消) / **V-A6 ✅ RAM 実測**: idle 10.4 MB → 再生中 160-244 MB(3.7Gi の 6.5%、24/7 常駐妥当性 OK)。**video-zoom=-0.05 オーバースキャン補正導入** ✅: TV 実機で「ちょうどいい」確認済。🟠 dashboard BGM mpv 同居 / 720p deinterlace / 単一 lane 再確認は残(別 TODO 化せず実運用観察に任せる)。5 秒周期 Live edge 追従 stall は別軸 → 下記 RV-8 TODO へ分離。

### F-video 残課題 (2026-05-28 RV-5 build 検証で発覚、別タスク分離)

- [x] ~~**RV-8: Live edge 追従 stall (5 秒周期カクつき) の root cause 切り分け**~~ — **2026-06-29 クローズ**。高 bitrate Live (3D 配信等) × Wi-Fi 2.4GHz の帯域上限が根本原因と切り分け済み (IPv6 100% loss 軸は yt-dlp 解決/DL 経路では RV-9 `force-ipv4=` / dlqueue `-4` で対処済、ffmpeg fragment 側は低 bitrate Live で stall ゼロの実測により否定的。切り分け経緯の詳細は git 履歴の本エントリ旧版参照)。mpv buffer 調整は的外れ (baseline 実測で cache 10 秒先読み余裕 + underrun false = fetch 速度律速)、真の対策は Wi-Fi 5GHz/有線化 or 480p 受容でいずれもユーザー環境変更。再現時に再起票する。

### F-video 追加要望（user 2026-05-21。bot 非依存 = v1-α 系列）→ 2026-05-22 実装完了（RV-6/RV-7）
- [x] **RV-6 URL 入力欄のクリアボタン**: `#video-idle` にクリアボタンを足し、`#video-url` を空に + 取りこぼし/失敗文言(`#video-ended`)もクリア。純 client UI、低リスク。
- [x] **RV-7 ±N秒スキップ + ステップ可変**: 再生中に「10秒進む / 10秒戻す」ボタン。上下ボタンでステップ秒数を可変（既定 10s）。
  - 設計メモ: 相対シークは **§3.4 verb whitelist に "seek abs+rel" として既に許可済**ゆえ RCE 面を増やさない。実装は (a) `/api/video/seek` に `relative` モード引数を足す（`seek <delta> relative`）か (b) client が `state.pos + delta` で既存 absolute seek を呼ぶ、のどちらか。(a) が素直（pos 取得待ちが要らない）。
  - ステップ秒数は **client 側 state**（localStorage 表示ヒント、token と分離）。サーバは delta を受けるだけ。
  - **live は is_live でシーク無効ゆえ ±N も非表示**（VOD/seekable のみ。§5.1 と整合）。
  - SW CACHE bump 必須。

### ニコニコ動画 残課題 (2026-06-11、allowlist 済み) → 2026-06-12 解決
- [x] **RV-9: ニコニコ動画の実再生 → 2026-06-12 解決** (user 依頼「ストリーミング再生はできてほしい」)。真因 = ニコニコ domand 配信は yt-dlp の **per-format `cookies` フィールド (domand_bid)** を要求するが **mpv 0.34.1 ytdl_hook は `cookies` フィールド未対応** → HLS 403。対処 = §8 手続きの 1 回の設計判断で **upstream v0.37.0 ytdl_hook.lua (cookies 対応済) を無改変 vendored** し、`--ytdl=no` + `--scripts=vendor/ytdl_hook.lua` で 0.34.1 core に載せる (選択肢比較・撤去条件 = mpv 0.37+ 更新時、は video-design §8 参照)。実機 = ニコニコ sm9 8s playing / YouTube VOD 回帰なし (seekable=True 維持) / 本番 TV 10s playing / 3 分 soak stall 0/38。なお当初の再判定材料だった「ニコニコ DL 19 分/5 分動画」は帯域制限ではなく **IPv6 接続 timeout が真因** (`-4` で 90 倍、上記最終更新 2026-06-12 ①参照) で、streaming 解決後もニコニコは快適に観られる。下記 Done 参照。
- live.nicovideo.jp (ニコ生) は今回スコープ外、将来候補。

### 動画まわり拡張ロードマップ (user 確定 2026-06-11、着手はこの順)
2026-06-11 の「リモコン動画再生パワーアップ」相談で確定した残り 2 件。着手時は「RV-10 やって」等の名指しで引く。

- [x] **RV-10: 事前ダウンロードキュー → 2026-06-12 実装完了** (設計 = `workspace/redesign/dlqueue-design.md` v1.1、実装・検証は下記 Done 参照)。ニコニコ sm9 の yt-dlp 直 DL 成功で **RV-9 の 403 迂回を実証済み** → RV-9 要否の再判定材料が揃った (下記 RV-9 追記参照)。
- [x] **RV-11: TVer 対応 → 2026-06-12 完了** (user 依頼「TVer の再生にも対応してほしい」)。手順どおり検証が先 = TVer platform API から実エピソード 3 本 (3 系列局) を取得し `-J` で解決検証 (DRM 0 / HLS / 無ログイン) → allowlist 拡張 + 本番実再生スモーク 11s playing。**RV-10 (事前 DL) との組み合わせは不可と裁定**: 期限付き見逃し配信のローカル保存は期限・権利の両面を迂回するため、`normalize_dl` で DL の門を分離し TVer を弾く (再生のみ対応)。下記 Done 参照。
- [x] **RV-12: プレイリスト再生キュー → 2026-06-18 完了 (TV 実機検証済)** (元の 2 件ロードマップ外、`video-design.md` §0 が punt 予告していた拡張を user 要望で着手)。mpv ネイティブ内部 playlist + flat-playlist メタ展開 (title は PWA 保持) で server stateless を崩さず実現。出口は list= 自動判定。**本番 TV 検証で 5 曲投入→次へ→自動 advance→前へ→jump→PWA 閉じて継続→再開 pos 復元 を確認**。実機で append エントリの pause バグを発見・修正 (append にも per-file `pause=no`、下記 Done 参照)。live 混在は手持ち無くスキップ (設計上は手動「次へ」誘導)。

### F-ytcheck: ytcheck フィードバック連携 (#65、2026-07-07 調査済・スコープ確定 → **同日 3 機能とも実装完了**)

user 起票 #65「ytcheck のフィードバックと巡回チャンネル編集をリモコンから」の調査結果と裁定。**案 B (①+②) は 2026-07-07 実装完了 (下記 Done 参照)**。③巡回チャンネル編集は #69 に分離 → **ytcheck 側の前提整備 (channel_store.py = flock + 1ch CRUD + git 自動 commit) は 2026-07-07 完了 (ytcheck commit 2419110、詳細 = ytcheck/docs/STATUS.md)** → **remote 側の編集 UI 接続 (#71) も 2026-07-07 実装完了 (commit d92241f、下記 Done 参照)**。

- 要求を 3 機能に分解: ①集計/レポート閲覧 ②視聴フィードバック記入 (視聴チェック + ○/×) ③巡回チャンネル編集。**スコープ = 案 B (①+②) を先行、③は #69 に分離** (2026-07-07 user 裁定)。
- **①の前提**: ytcheck のレポート/viewing ファイルは既に `vault/notes/ytcheck/` に出力済み (`ytcheck/run.sh` が `YTCHECK_WRITING_DIR` / `YTCHECK_VIEWING_DIR` を固定) → 既存「ノート」タイルで生ファイル閲覧は今日から可能。専用タイルは月次集計 `feedback_report.build_report()` を返す GET 1 本で足りる。
- **②の設計論点**: `viewing-YYYY-MM.md` の該当 video_id 行の `[ ]`→`[x]` と `[feedback: ]`→○/× 書き換え API。**remote 初の外部プロジェクト write** (書き込み先は vault/notes/ 配下 = 機械書き込みの正規領域内)。R3 のとおり cross-process ロックが必要 (ytcheck timer は毎朝 05:00 に同ファイルへ追記)。行パーサは `feedback_report._LINE_RE` を流用可。
- **プロセス境界**: ytcheck は専用 venv (依存多数) だが `tools/feedback_report.py` は stdlib のみ・config 非依存 (import 一覧を一次確認済) → remote (stdlib のみ) から sys.path 追加で直接 import 可、subprocess 不要。
- **③を分離した根拠**: 対象 `ytcheck/tasks/youtube-channels.json` は vault 外 (ytcheck repo の git 管理下) + 毎朝 05:00 実行が subscriber_count を同 JSON に書き戻す (`main.py`、当時ロックなし) → remote 側だけロックしても競合は防げない。ytcheck 側に flock + 1ch 単位 CRUD を先行実装し、repo 内 JSON への機械編集の git diff 運用を決めてから remote を繋ぐ 2 段構え (#69 → 前段 2026-07-07 完了)。**#71 (remote 側接続) の前提**: `channel_store.py` を feedback_report と同様 sys.path append で import (stdlib のみ・config 非依存は担保済み)。書き戻しは merge 方式 (2 フィールド限定) になったため remote 編集との lost update は構造的に解消済み。git diff 運用 = 書き込み後自動 commit (ytcheck/docs/STATUS.md 参照)。
- 実装時に触るファイル (思考ログタイルの定型 5 点セット): `server/ytcheck.py` (新規、read + viewing write) / `app.py` (import + ハンドラ + ROUTES、write は remote 初の POST 外部書き込み) / `index.html` (タイル + セクション + tile-count) / `app.js` (SCREENS / openScreen / init / render) / `sw.js` CACHE bump。

### v1-β サブタスク（6/2 観察窓後、bot.service restart 伴う）

**2026-06-01 当面保留**: companion 全体が Phase 3 を畳んで Phase 4 (相棒層) に進む方針に転換。Phase 4 着手条件 #2 を『Telegram 移行 (5/28) を最後の bot.py 大改変として 6/11 判定』に戻したため、その前提として当面 bot.py を大きく触らない方針。remote v1-β (F-1 = bot.py 双方向化、RB-1/2/3) は Phase 4 着手の見通しが立つまで**着手保留**。設計内容は破棄せず、着手タイミングのみ後ろ倒し。F-2 voice / F-3 OS status (v1-α、bot.py 非依存) は影響なし。根拠: PROJECT.md 健全性履歴 2026-06-01 entry。

**2026-06-01 追加判断で前提更新**: 上記「6/11 判定まで当面 bot.py 凍結」の前提が変わった。同日 user 判断で **条件 #2 (2 週間観察) を Phase 4 着手の門から外す (bot.py 大改変時の様子見へ再定義)、Phase 4 は条件 #1 (vault 充足) + #3 (user 宣言) で着手可** となったため、remote v1-β は「凍結 (6/11 判定まで)」ではなく **Phase 4 着手後に bot.py 大改変として、その変更への様子見観察付きで入れる** 位置づけ。すなわち凍結ではなく「Phase 4 のキャラ作業を優先した順序の後ろ倒し」。F-2/F-3 (v1-α) は引き続き影響なし。根拠: PROJECT.md 健全性履歴 2026-06-01 entry「追加判断」section。

- [ ] RB-1: bot.py 最小双方向化(REMOTE_SOCKET=$XDG_RUNTIME_DIR/companion-bot-remote.sock 0o600, _handle_remote verb=ask/status, REMOTE_CHANNEL_ID≠0, budget guard 経由必須=M-14 核心, 既存無改変, tests 追加, restart は観察窓後)
- [ ] RB-2: remote F-1 bridge POST /api/ask(非同期ジョブ型, job_id, polling, .state/jobs/, lane 単一 in-flight ガード 409, --resume リトライ厳禁, socket timeout=bot 最長 tier 導出 + claude_lock 待ち考慮[R2])
- [ ] RB-3: F-1 PWA UI(送信→polling→結果, job_id localStorage 復元, 多重投入 disable)+ F-3 集計を verb=status に接続(glance に quota%/最終活動昇格)
- [ ] RB-4: docs/STATUS.md + remote-design.md 反映 + 健全性観察影響記録

### 実装メモ(LOW, 該当サブタスクで吸収)
- R3: 将来 F-4 vault write の endpoint を切る時、cross-process vault lock が要る(bot の in-process vault_lock では不足)
- R4: PWA の token 失効後 401 再入力 UX

## In progress

(なし)

## Done

- 2026-07-14 **#105 やることの完了ボタンを撤去 = 完了操作を AI 側 tickets.py done に一本化・commit 77168b1**: user 起票「編集と完了が近い。編集中に完了が押せてしまう。完了は別メニューもしくは ai 側で操作でもいい気がする」。原因構造 = 編集/完了ボタンが gap 6px で隣接 (高さ ~25px、タップターゲット推奨 44px 未満) な上、コピー・編集は誤タップ無害なのに**完了だけ確認なし・UI から復活手段なし (履歴は閲覧専用) の非可逆操作**という非対称。対策 4 案 (①完了直後 undo 導線 + 間隔調整 [推奨提示]、②確認 2 タップ、③完了ボタン撤去 = AI 移管、④CSS 間隔調整のみ) を AskUserQuestion で提示し **user 裁定 = ③ AI 移管**。着手前の裏取り = 完了経路は手元セッションと Telegram #chat の両方で `tickets.py done` が許可済み (bot-workspace/.claude/settings.json:75 + CLAUDE.md「#N 終わった」運用 2026-06-11 OWNER 依頼) で成立、bot.py 内の tickets 読み取り限定は proactive/reminder 自動機能のみの境界で対話経路に影響なし。実装 = PWA: renderTodo の完了ボタンと doneTodo を削除 (UI は起票・編集・コピーのみ)、List カードに完了方法の `note` 1 行 (「完了にするには AI に『#N 終わった』と伝える」)。server: 使い手ゼロになった `POST /api/todo/status` を削除 (`tickets.set_status` 自体は CLI start/done が使うため残置)。SW v42→v43。**再検討用メモ**: /api/todo/status は STATUSES 任意遷移を受けたため undo 導線 (①案) はサーバ改修ゼロで実装可能だった — AI 移管運用が合わなければ 77168b1 の revert + ①案で引き直す。検証 = 153 tests pass + node --check/ast.parse + code-reviewer 修正必須 1 件 (SW bump、指摘前に反映済み)・軽微 1 件 (restart 明示 → 実施) + 本番 restart 後 loopback (health 200 / 削除 route POST 404 / 既存 /api/todo・/api/todo/edit 無トークン 401 維持)。**Pixel 6 実機確認 2026-07-14 完了 (user「実機確認できた OK」) = #105 クローズ**。
- 2026-07-13 **tickets CLI の --help 誤起票対策・commit 2b33f57**: `add --help` が `--help` を本文として誤起票する事故の再発防止 (直近 #88、user 指摘)。①`main` 冒頭で `help`/`-h`/`--help` をどの位置でも捕捉し usage 表示のみ (state 非接触・rc=0、subcommand ごとの分岐を作らない入口 1 ゲート)。②`_parse_add` は `--by` 以外の `--` 始まりトークンを `TicketError` で reject (トークン先頭のみ判定、quoted 引数の途中の `--語` は通る)、`--by` 値欠落も reject (従来は本文に混入)。③usage 抽出 `split("CLI", 1)` が docstring 1 行目「+ CLI。」に誤マッチして全文が出ていた既存の粗を `_usage()` ヘルパーで修正 (help 経路と else 分岐で共用)。server API 面 (app.py が使う add/edit/set_status/active 等) は不変 = **restart 不要**。検証 = 153 tests pass (TestParseAdd 6 + TestMainHelp 3 追加) + 実 CLI で `add --help` → usage/rc=0/未起票・`add --by` → error/rc=2 を実測。code-reviewer 修正必須ゼロ (軽微 1 件 = unquoted 本文中の独立 `--help` トークンは usage になる argparse 同型挙動、認識共有のみで修正不要)。
- 2026-07-10 **#77 ytcheck フィードバックに URL 貼り付け記入を追加 (PWA のみ)・commit 002a6ea**: ytcheck 画面上部に「URL で記入」カード追加。YouTube URL を貼り付けて ○/× タップで視聴チェック + フィードバックを一括記入。サーバー無変更 = 既存 `/api/ytcheck/feedback` を `checked: true` + `feedback` 同送で利用。`ytParseVideoId` は `youtu.be` / `?v=` / `shorts|live|embed/` / 裸 11 文字 ID の 5 パターン対応。当月→前月フォールバック (月替わり直後にも対応)、推薦外は「推薦一覧にありません」、通信障害は「記入に失敗しました」と区別。SW v41→v42。code-reviewer 修正必須ゼロ・軽微 3 件反映 (var→const/let / イベントリスナー登録順を既存の後に / netErr フラグで通信障害と not-found を分離)。restart 実施済。**Pixel 6 実機確認 2026-07-10 完了 (user「確認できた OK」)**。
- 2026-07-10 **#76 vault 一覧に全フォルダ横断の更新順モードを追加・commit d421837 + b121b09**: server: `list_notes()` の各 note dict に `mtime` (`int(os.stat().st_mtime)`、OSError 時 0) を追加。PWA: 検索欄下にフォルダ/更新順のトグルバー (`localStorage` 永続)。更新順は全フォルダの notes を flat 化→mtime 降順、各行に `fmtDateTime` で日時併記。検索中はモード非適用 (検索クリアで選択モードへ復帰、キャッシュ済みデータから再描画 = 再フェッチ不要)。後方互換 = 旧 JS は余分な mtime を無視、新 JS が旧 API を受けた場合は mtime=0 で安定表示。SW v40→v41。検証 = 単体 142→144 件全 PASS (mtime 存在 + 書き換え後の mtime 大小) + node --check OK + code-reviewer 修正必須ゼロ・軽微 3 件 (docstring=反映、vaultLoaded 統一・検索中ボタン=任意見送り) + restart 実施 + loopback health 200 / vault/list 401。**Pixel 6 実機確認 2026-07-10 完了 (user「確認できた OK」)**。
- 2026-07-08 **#59 YouTube Music 再生時専用 UI = NowPlaying TV (B案) クローズ (台帳反映漏れの後始末)**: 実装は 2026-06-30 完了 (commit 97f91c9) だったが STATUS.md 未記載・チケット #59 未クローズのまま残っていた。機能 = music.youtube.com URL を再生すると mpv は per-file `vid=no` (映像抑止) で音声のみ再生し、Firefox kiosk (専用 profile で通常 Firefox と分離) が HDMI-1 に NowPlaying 画面を全画面表示 (曲名・アーティスト・アルバムアート・プログレスバー・時計、2s ポーリング)。停止 or 非 music URL 再生で Firefox kill → デスクトップ復帰。server = `nowplaying.py` (Firefox ライフサイクル管理) + `/api/video/np` (認証不要) + video.py の music flag (play / play_playlist_load)、PWA 側 UI 変更なし。後続 fix 2 件 = レビュー指摘 (CSS @import 順序 + import 整列、69c254a) / kiosk 起動直後の fullscreen を一旦解除→HDMI-1 へ移動→再付与 (Marco がフルスクリーン窓の移動要求を無視してモニター側に張り付くため、ba242c0、2026-07-06)。**2026-07-08 TV 実運用確認 OK (user「問題なく動作してた」) = #59 done (edit でクローズ理由追記済)**。本エントリはコード変更なし・restart 不要。
- 2026-07-08 **#71 実機フィードバック対応: 独立画面化 + 操作結果の表示位置修正・commit 9509399 (PWA のみ)**: Pixel 6 実機確認で「操作は良さそう。削除・変更 OK、登録は無反応。最下部だと存在を忘れるので別メニュー化か上部にボタンを」。
  - **「無反応」の原因調査**: 本番ログは log_message 抑止で残らないため状況証拠で確定 = 削除 3 件・更新 1 件は ytcheck repo に自動 commit 済み (機能は本番動作)、add は痕跡ゼロ。エラー文言が一覧最上部の #yt-ch-msg 固定で、追加フォーム (53ch の下) から viewport 外 → @handle 共有 URL → 400 の文言が見えず「無反応」に見えたと判断。
  - **対応**: ①追加の結果/エラーはボタン直下 #yt-ch-add-msg (成功「追加しました」)、@入力は送信前にその場で「@ハンドル形式は使えません」、保存/削除/日数バリデーションはフォーム内 result (reviewer 指摘 = add 側だけでは同じ障害モードが下方の行の編集に残存、2 周目防止で同 commit)。②#ytchannels 独立画面 (SCREENS/openScreen/popstate 3 点セット)、入口はフィードバック画面上部の data-open「巡回チャンネルの編集 ›」、戻るでフィードバック画面復帰。ホームタイル不変。折りたたみ機構 (toggle/caret/hidden) は純削除。SW v39→v40。
  - **検証**: 単体 142 pass (server 無変更) / 隔離 Playwright E2E 18/18 (フォーム内文言の viewport 内表示・戻る 2 段含む、pageerror 0) / スクリーンショット目視 / code-reviewer 修正必須ゼロ・軽微 1 件反映。restart 不要 (web のみ、SW bump で到達)。**Pixel 6 実機確認 2026-07-08 完了 (user「確認できた OK」) = #71 一式クローズ**。
- 2026-07-07 **#71 巡回チャンネル編集 UI の接続 (#69 後段) 実装・commit d92241f**: ytcheck の巡回チャンネルリスト (`ytcheck/tasks/youtube-channels.json`、56ch) を PWA から閲覧・編集・追加・削除。
  - **A. server/ytcheck.py 追記**: `channel_store.py` (ytcheck 側、stdlib のみ・config 非依存) を `sys.path.append` で import 流用 (feedback_report と同型。youtube_checker/ 直下には config.py 等も並ぶが append のため server/ 側の解決が常に勝つ)。remote 側は入力検証 + HTTP 写像のみで、排他 (flock) / atomic write / ytcheck repo への git 自動 commit は全て store 側の責務 = 書き込みロジック二重実装なし。`parse_channel_id` は素の UC ID (`UC[0-9A-Za-z_-]{22}` fullmatch) か youtube.com (www/m 含む) の `/channel/UC〜` URL のみ受理。**@handle は yt-dlp subprocess なしに解決できないため非対応の設計判断** (UI に取得手順を明記)。パスは `REMOTE_YTCHECK_STORE_DIR` env で上書き可。**ytcheck repo を動かすと import 失敗で remote が起動しなくなる点は feedback_report と同じ**。
  - **B. app.py**: GET `/api/ytcheck/channels` (JSON 素通し) + POST `/api/ytcheck/channel/{add,update,delete}` (全 Bearer 必須)。`_channel_fields` で favorite (int 1-5、bool 除外) / check_days (int 1-30 = ytcheck config Field と同値域) / note (str ≤500) / genre (**JSON の genres キーが正** = コード側に定数を持たない) を検証。add は name (≤200)・genre 必須、check_days/favorite 省略時 4/3。例外→HTTP は store 契約と 1:1 (重複 ValueError→409 / 不在 KeyError→404)、channel_id は immutable で fields に決して入れない。
  - **C. PWA**: `#ytcheck` 画面内の折りたたみ「巡回チャンネル」(タイル増やさず視聴フィードバックと同居)。ジャンル定義順の見出し + 各行 name/★favorite/日数/note、行タップで編集フォームを 1 件だけ展開 (select ★1-5 / number 1-30 / genre select / note maxLength 500)、保存・削除 (confirm、dl 削除と同型)・追加フォーム。POST 成功 → 一覧 GET 再取得で全再描画 (56ch 程度、差分更新は持たない)。開いた時のみ取得 = 15s ポーリング非搭載。genres 未定義の genre 現値 (JSON 手編集後の「その他」行) は「（未定義）」option で維持し、保存時は genre をペイロードから省く = 無警告のジャンル移動と server 400 の両方を回避。createElement + textContent のみ維持。SW v38→v39。
  - **検証**: 単体 130→142 件 pass (parse_channel_id 受理/拒否 + CRUD 委譲 12 件、tmpdir 隔離 = repo 外で git commit は warning のみの契約も確認) / 隔離インスタンス (別ポート 47899 + 隔離 tokens + env ダミー JSON、本番非接触) HTTP 実観測 24/24 (401 無トークン・誤トークン / add 素ID・URL 形式・重複 409・不正入力 400 系 7 種 / update 反映 + 非対象フィールド不変 / delete / JSON 実体確認 / 既存 viewing 回帰) / Playwright + Chromium 412x915 E2E スモーク 15/15 (隔離実 API 相手、confirm accept/dismiss 両方、pageerror 0) / code-reviewer 修正必須ゼロ・軽微 3 件反映 (placeholder https 明記 / note maxLength / 未定義 genre 保護) / 本番 restart 後 loopback (health 200 / 新 4 route 無トークン 401 = 404 でない / 偽 route 404、本番 JSON への write なし)。
  - **裁定メモ**: チケット指示「編集可能フィールドの範囲は着手時に user と確認」は AskUserQuestion 無応答のため**推奨案で独断** = favorite/check_days/note + genre 編集可 (name 編集は対象外)、追加/削除あり、@handle 追加非対応。変更したければ次セッションで指示 (server 検証は allowlist 構築なのでフィールド追加は安価)。
- 2026-07-07 **F-ytcheck: ytcheck フィードバック連携 実装・commit (#65 案 B、user 裁定済みスコープ)**: 月次集計レポートのタイル表示 + `vault/notes/ytcheck/viewing-YYYY-MM.md` への視聴チェック/○× 記入 API。巡回チャンネル編集は #69 に分離済み (対象外)。
  - **A. server/ytcheck.py (新規)**: ytcheck の `tools/feedback_report.py` (stdlib のみ・config 非依存) を `sys.path.append` で import 流用 (`build_report` / `parse_viewing_text` / `_LINE_RE` / `_FEEDBACK_RE` / `_VIDEO_ID_RE`。パーサ二重実装なし、subprocess 不要)。`get_month(month)` = report + entries [{channel, video_id, title, score, checked, feedback}] (title は行から抽出、read-only でロック不要)。`set_feedback(month, video_id, checked?, feedback?)` = flock 下の read→行書き換え→write で 1 回確定。パスは `REMOTE_YTCHECK_VIEWING_DIR` / `REMOTE_YTCHECK_CHANNELS_JSON` / `REMOTE_YTCHECK_TOOLS_DIR` で上書き可の固定デフォルト (thoughts.py の REMOTE_THOUGHTS_PATH と同型)。**注意: ytcheck repo (`~/companion/ytcheck/python/youtube_checker/tools/`) を動かすと remote は import 失敗で起動しなくなる** (env で追随可)。
  - **B. remote 初の外部 write の境界**: 書き込み先は viewing ディレクトリ配下の該当ファイル 1 点に閉じる。パスは `\d{4}-\d{2}` fullmatch 済み month からのみ組み立て (traversal 防止)、video_id はパスに入れず既存行との照合のみ (必ず re.escape)。feedback は ○/×/空 (取り消し) の 3 値のみ受理、checked は bool のみ。分岐は state を引く前に確定 = 入力検証 400 → 残る YtcheckError は「ファイルなし/該当行なし」= 404 と一意 (文言マッチなし)。feedback 欄の無い過去月フォーマット行は末尾に付加 (feedback_report の後方互換パースと整合)。
  - **C. cross-process ロック (R3 消化)**: viewing ディレクトリ直下の共有 `.viewing.lock` を remote (`set_feedback`) と ytcheck timer (`output_formatter.update_viewing_history`、毎朝 05:00 の read→全文再構成→write) の**両プロセス**が flock(LOCK_EX) で囲む。remote 側だけのロックでは lost update を防げないため (R3 の趣旨)、ytcheck repo 側にも同一パス導出の flock を追加 (ytcheck repo commit + ytcheck/docs/STATUS.md 追記)。blocking 取得・timeout リトライ/エラー分類分岐なし (設計上限ルール)。
  - **D. app.py**: `import ytcheck` + `api_ytcheck_viewing` (GET、month 省略時は当月、不在月は entries 空 + 集計 0 の 200 = 月ナビで自由に動ける) + `api_ytcheck_feedback` (POST) + ROUTES 2 行 (両方 Bearer 必須)。
  - **E. PWA**: ホーム grid にタイル `✓ 10 YT巡回 / 視聴チェック・集計` (tile-count 9→10) + `#ytcheck` 画面 = 月ナビ (前月/翌月、当月デフォルト) + 月次集計レポート (折りたたみ pre、サーバ生成テキストを textContent でそのまま) + チャンネル別動画一覧 (視聴チェックトグル + ○/× ボタン、再タップで取り消し = 空文字送信、タップ → POST → 返却 entry で再描画)。開いた時 / popstate / 月ナビでのみ取得、15s ポーリング非搭載 (思考ログと同型)。createElement + textContent のみ (innerHTML 不使用の規律維持)。POST 多重発火は ytBusy ガード、POST 中の月ナビは返却時に月一致チェックで表示を守る。style.css に .yt-* 一式。SW CACHE v37→v38。
  - **検証 (本番非汚染)**: ①単体 108→130 件 pass (test_ytcheck.py 22 件追加 = month 検証 / 行書き換え (チェック・○×・取り消し・過去月付加・他行不変・末尾改行維持) / video_id regex escape / 該当なし・ファイルなし YtcheckError / ロックファイル生成 / **ロック保持中の write ブロック実測** (flock 先取り → ブロック → 解放で完走))。②ytcheck 側 229 件 pass (flock 追加後、tests は mock.patch の tmp_path 上で回帰なし)。③`node --check` app.js / sw.js OK。④隔離インスタンス (scratchpad へ server/web コピー = .state 隔離 + 別ポート 47899 + 隔離 token + env ダミー viewing dir、本番 .state/本番 vault 非接触) で 17/17 PASS = authed GET 200 (report「掲載動画数: 2」+ entries) / POST で `[ ]`→`[x]` と `[feedback: ○]` の**ファイル実書き換え** / 無トークン 401 (GET/POST) / 不正 month 400 (GET/POST) / 不正 feedback 値 400 / 該当 video_id なし 404 / checked・feedback 両方なし 400 / month 省略 200 / 既存 route 回帰なし。⑤Playwright + Chromium UI スモーク (web/ を使い捨て node http 配信 + /api 全モック、本番非接触、412x915) 19/19 PASS = タイル→画面遷移 / entries 描画 (チャンネル見出し・タイトル・既存チェック/○) / レポート折りたたみ / チェック・○× タップの POST body 検証と再描画 / ○ 再タップで取り消し送信 / 月ナビ (前月空表示→翌月復帰) / 戻る / pageerror 0 + スクリーンショット目視。⑥本番 restart 後 loopback = /api/health 200 / 新 route 2 本とも無トークン 401 (404 でない) / 既存 route 401 維持 (本番への write なし、GET まで)。
  - restart 実施済 (server/*.py 変更のため必須)。push 不要 ((C) ローカル git のみ)。**Pixel 6 実機確認 2026-07-07 完了 (user 確認「よさそう」、問題なし)**。code-reviewer 軽微 2 件のうち ①feedback_report の流用名の逆リンクは ytcheck/docs/STATUS.md に追記済 (ytcheck d5a0ebd)、②write の tmp+rename 化は既存慣習と一貫のため対応不要と判定。

- 2026-07-07 **やること画面にチケットのコピーボタン追加・commit (user 依頼)**: 「ターミナルに切り替えてる間に忘れちゃうからコピーするボタン追加してほしい」。変更は web/ 2 ファイルのみ (app.js / sw.js)、server 非変更。
  - **UI**: renderTodo の各チケット行に「コピー」ボタンを追加 (コピー→編集→完了 の順、既存 .todo-done クラス流用で CSS 無変更 = 部分キャッシュ混在でも壊れない)。完了履歴側は対象外 (用途 = 未対応チケットをターミナルへ持っていく)。
  - **copyTodo**: `navigator.clipboard.writeText("#" + t.id + " " + t.text)` = `#N 本文` 形式 (**→ 同日 11de0a4 で `#N` 単独に変更、下記フォローアップ第 2 項参照。以降この形式記述は履歴**)。ターミナルの claude セッションに番号ごと貼れば「#N やって」運用 (tickets.py show N) にそのまま乗る。click ハンドラ内の直接呼び出しで user gesture 要件を満たし、tailscale serve HTTPS = secure context で clipboard API 利用可。成功「済」/失敗「失敗」を 1.2s 表示して「コピー」へ復帰 (try/catch 1 回、リトライ・エラー分類分岐なし = 設計上限ルール準拠)。
  - **検証**: node --check 構文 OK + Playwright + Chromium (web/ を使い捨て http.server 配信 + /api 全モック + clipboard 権限 grant、本番サーバ/.state/token 非接触) 8 アサーション全 PASS = チケット 2 件描画 / ボタン並び / clipboard 実読み「#12 テスト用チケット本文」/ 済→コピー復帰 / doing 側コピー / 編集回帰 / pageerror 0。code-reviewer = 修正必須ゼロ (15s ポーリング再描画・startEditTodo の replaceWith とも detach 済みノードへの setTimeout 書き込みのみで無害、innerHTML 不使用維持を確認)。
  - SW CACHE remote-v34→v35。restart 不要 (web のみ、SW bump で PWA へ到達)。commit 75aa755。push 不要 ((C) ローカル git のみ)。
  - **フォローアップ (同日、Pixel 6 実機 user フィードバック)**: 「3 つのボタンが本文を圧迫してる」→ ボタン列を 2 行目へ落として右寄せ (CSS のみ)。`.todo-item` に flex-wrap:wrap、`.todo-actions` を flex:0 0 100% + justify-content:flex-end。本文は 1 行目全幅 (412px で約 160px→308px)。dl-item はボタン li 直付けで無影響・編集モードも input 全幅 + ボタン下段・完了履歴は actions なしで無影響 (code-reviewer 実コード裏取り、修正必須ゼロ・軽微 1 件 = flex none+basis を shorthand 統合、反映済)。Playwright 412x915 で 7 アサーション全 PASS (下段配置・本文幅 308px・右寄せ・非重複・編集モード・pageerror 0) + スクリーンショット目視。SW v35→v36。commit ac0167c。**Pixel 6 実機確認 2026-07-07 完了 (user 確認「よさそう」、コピーボタン + 2 行レイアウトとも問題なし)**。ticket #68 done でクローズ済。
  - **フォローアップ 2 (同日、user 裁定)**: コピー形式を `#N 本文` → **`#N` 単独** に変更 (「補足もあるから番号だけがいい」。本文は claude 側が show N で実物を引くため冗長、番号の後ろに補足を打ち足す運用)。素の `#N` がコンテキスト空のセッションで通じるかの user 懸念には **workspace/CLAUDE.md「チケット番号 (#N) の受け方」新設** (常時読み込みで「#N で始まるメッセージは show N で実物を引く」を保証、workspace repo commit) + memory `project_companion_shared_todo` / MEMORY.md 索引の同期で対応。Playwright 8 アサーション全 PASS (clipboard 実読み "#12"/"#13"、文言遷移・編集回帰・pageerror 0) + code-reviewer 修正必須ゼロ (軽微 2 件 = copyTodo 引数絞りは不採用・STATUS.md 旧仕様記述の追随は本更新で消化)。SW v36→v37。commit 11de0a4。

- 2026-07-07 **F-vault ビューア可読性改修 + 表示されない画像の修正・commit (user 依頼)**: 変更は web/ 4 ファイルのみ (index.html / app.js / style.css / sw.js)、server/*.py 非変更。実装は implementer subagent (model inherit)、レビューは code-reviewer subagent (修正必須ゼロ)。
  - **画像が表示されない原因 (全数調査)**: vault 105 ノートの画像記法を集計。`![[name.jpg]]` wikilink 4 件と pbs.twimg.com 約20件は既存経路で表示済み。壊れていたのは ①CSP `img-src 'self' blob: https://pbs.twimg.com` が st-note / publish.obsidian.md / abs-0.twimg.com (絵文字 SVG) 等 約17件をブロック ②YouTube/x.com の動画 URL を `![](…)` で埋め込んだ 8 件 (Obsidian では動画埋め込みになる記法、ビューアでは恒久壊れ img)。
  - **A-1 CSP 拡大**: img-src を `'self' blob: https:` に。script/style/object-src/base-uri は不変 (画像ソースのみ緩和、理由コメントを index.html に併記)。
  - **A-2 error フォールバック**: 描画後 `img:not([data-vault-img])` (外部画像のみ、blob 画像は既存 catch が処理) に once の error リスナー → 「▶ <hostname> で開く」`target=_blank rel="noopener noreferrer"` リンクへ replaceWith。href は http(s) 限定で data: 等は img.remove() (code-reviewer 軽微指摘を反映、既存 1274 行のイディオムに整合)。リスナー付与は vaultLoadImages の属性除去より前 (同期実行で error イベントに必ず間に合う)。
  - **B-1〜B-5 可読性**: breaks:true (Obsidian の 1 改行=改行) / タスク checkbox 復活 (1117 箇所。FORBID_TAGS から input のみ除外 + 後処理で type=checkbox 以外 remove・disabled 強制・task-list-item 付与。form/textarea/button/style の FORBID は維持) / 本文ヘッダをフォルダ小 + ノート名大の 2 段化 (#vault-doc-path、ルート直下は非表示) / 一覧を details/summary 折りたたみ (既定全閉 + 件数表示、開閉状態は vaultOpenFolders Set に toggle で記録し再描画で復元、検索結果はフラット維持。`.vault-folder:first-child` は summary 化で全見出しに誤爆するため `.vault-folder-group:first-child > .vault-folder` に変更) / 検索ヒット語 mark 化 (vaultAppendHighlighted = indexOf 分割 + textNode/createElement、大小無視・全出現、name と snippet 両方)。
  - **XSS 規律の維持**: innerHTML は DOMPurify 通過後の 1 箇所 (vaultRenderMarkdown) のまま。B-5/A-2 とも文字列連結 HTML なし。B-5 の空 needle はガード済・indexOf ベースで正規表現メタ文字は無害 (code-reviewer 確認)。
  - **検証**: vendored marked の breaks / タスクリスト出力形状を node で実測 (li に class を付けない → JS 付与で対応) + Playwright + Chromium で web/ を静的サーブし実ブラウザ 24 アサーション全 PASS (checkbox 残存/type=text 除去/checked 維持、外部画像失敗 3 件のリンク置換、CSP violation ゼロ、ヘッダ 2 段、折りたたみ開閉復元、mark 全出現) + スクリーンショット 4 枚目視 + 既存単体 108 件 pass (server 非変更で回帰なし) + `node --check` OK。
  - SW CACHE remote-v33→v34 (bump 忘れ = PWA 不達の罠を消化)。restart 不要 (server 非変更)。commit 5604ed8。push 不要 ((C) ローカル git のみ)。**Pixel 6 実機確認 2026-07-07 完了 (user 確認、問題なし)**。
  - **残課題 (任意、未着手)**: A-3 YouTube URL の img.youtube.com サムネイル化 (A-2 でリンクにはなるため保留) / 一覧の最近更新順 (server の list_notes に mtime 追加が必要)。

- 2026-06-20 **思考ログタイル (bot の私的思考ログを read-only タイムライン閲覧) 実装・commit (user 要望)**: bot の `companion_thoughts.jsonl` ({timestamp, observation} 追記式) を PWA から時系列で「そっと覗く」タイルを 1 枚追加。**軸4拡張 機構1 の思想境界を表示側でも厳守**するのが肝 (思考ログ = 読まれない前提で書く = 観測されない領域がある = 別の心がある感の源 / 不在の核)。両立条件を全て満たす: **①bot からプッシュ・通知・新着バッジ・未読件数を一切付けない** (他 read タイルと同じ控えめな佇まい)、**②read-only 厳守** (編集・削除・既読・お気に入り等の双方向操作なし、開いて読むだけ)、**③サーバ無加工** (bot が書いた観察行をそのまま透過、解釈・要約・感情ラベル付けをしない。timestamp の読みやすい日時化のみ可で本文は不可触)。
  - **A. server/thoughts.py (新規) = read 専用データ層**: `list_entries()` が JSONL を 1 行ずつ `json.loads`、壊れ行・空行・非 dict 行はスキップして全体を落とさない (行単位頑健性)。最新が上 (ファイル末尾が最新の追記式 → reverse) で `MAX_ENTRIES=200` cap (vault search の SEARCH_MAX_RESULTS=100 に倣うレスポンスサイズ頭打ち)。**ファイル不在 (bot 発火前は実体なし) / 空 / 全行壊れは graceful に `{count:0, entries:[]}`** (OSError も含めて空扱い、他タイルを巻き込まない)。open は "r" のみ (書き込みモードを増やさない)。path は固定読みで `REMOTE_THOUGHTS_PATH` で上書き可 (既定 `../../bot/sessions/companion_thoughts.jsonl`、env で与えた 1 ファイルをそのまま open = vault.py の traversal 検証は固定パスゆえ不要)。timestamp / observation は obj.get でそのまま透過 (無加工)。
  - **B. server/app.py = endpoint**: `import thoughts` (他 import と同位置) + `api_thoughts` ハンドラ (他 api_* と同書式、`return 200, thoughts.list_entries()`) + ROUTES に `("GET","/api/thoughts"): (api_thoughts, True)` (Bearer 必須、既存 read 系と同じ)。書き込み endpoint なし。
  - **C. PWA (web/index.html + app.js + style.css)**: ホーム grid にタイル `… 07 思考ログ / そっと覗く` (data-open、新着バッジ DOM なし)、tile-count 6→7。`#thoughts` section = 縦ライン timeline (`renderThoughts` = createElement のみ、innerHTML 不使用)。**timestamp は従属表示** (小さく grey uppercase tabular、`fmtThoughtTime` が epoch 秒/ミリ/ISO を best-effort 整形・解釈不能は原文透過、本文には触れない)、**observation を主役** (ink 本文・行間 1.7・white-space:pre-wrap で改行保持、textContent 挿入)。空/発火前は「まだ何もありません」の静かな空表示。開いた時 / popstate 復元時に `refreshThoughts` で取得 (15s ポーリングには乗せない = push 装置を作らない)。SW v25→v26。
  - **検証 (本番非汚染)**: ①既存単体 100 件 pass (回帰なし) + `node --check` で app.js/sw.js 構文 OK + thoughts.py import + 不在時 `{count:0}` 確認。②本番サーバ restart 後 loopback curl = `/api/health` 200 / `/api/thoughts` 無トークン 401 (route 存在 + auth-gate、404 でない) / 既存 7 route (status/version/todo/dl/vault/video.state/thoughts) すべて 401 維持 + 偽 route 404 (回帰なし)。③**隔離インスタンス** (別ポート 47833 + 隔離 tokens.json + `REMOTE_THOUGHTS_PATH` でダミー JSONL、本番 .state/本番 thoughts 経路 非接触) で authed 200 = 3 件を最新が上で返却 + 壊れ行/空行スキップ + epoch/ISO 混在 timestamp 透過 + 誤トークン 401 + ファイル削除で `{count:0}` を実観測。検証用ダミー JSONL は確認後削除 (bot 書き込み経路 = bot/ 側は一切非接触)。
  - **思想境界の遵守**: プッシュ/通知/新着バッジ/未読件数ゼロ、read-only (双方向操作 endpoint・UI なし)、サーバ無加工 (透過のみ、整形は timestamp の表示だけ)。bot 側 (書き込み経路 / interests.py / bot.py) は不変更、remote 側で読むだけ。
  - commit = 本コミット。push は不要 ((C) ローカル git のみ、GitHub remote なし)。

- 2026-06-18 **RV-12 プレイリスト再生キュー (server + PWA) 実装・commit (user 要望: プレイリスト URL で ①中身が見える ②次/前 ③1 曲終わったら自動で次へ、PWA を閉じても継続)**: `video-design.md` §0 が「キュー/プレイリストは MVP punt」と予告していた拡張。**設計の背骨 (§1 server stateless / 再生 state は mpv 所有 / 監視スレッド不要、§3.4 verb whitelist) を崩さず実現**するのが肝。採用方針 = mpv ネイティブ playlist (`loadfile append` + `keep-open=no` の自動 advance) + `--flat-playlist` メタ展開で title は PWA 保持。
  - **A. server/playlist.py (新規) = flat 展開**: `yt-dlp --flat-playlist -J -I 1:100 --ignore-config -4 --no-warnings` を `start_new_session` + timeout 60s + `killpg` で実行 (dlqueue.py 同型の env 隔離)。`parse_entries` は純関数で entries 抽出 + single video フォールバック (entries 無し → data 1 件) + 各 url を `urlguard.normalize` 通過分のみ採用 (C4: reject は 1 件 skip、展開全体を fail させない) + title sanitize (制御文字 strip + 200 字 cap) + 先頭 100 件 cap (C3、yt-dlp `-I` と二重ガード)。成否は rc + JSON parse の 1 回確定 (stderr 文言分岐なし)。展開 Lock で二重投入を単一化。返り値 `{entries:[{url,title}], total, loaded}`。
  - **B. server/video.py = playlist verb**: `play_playlist_load(urls)` = 先頭 `loadfile replace pause=no` → 残り `loadfile append` を既存 `_send` のマルチコマンド機構で 1 接続送信。`queue_next`=`playlist-next` / `queue_prev`=`playlist-prev` / `queue_jump(pos)`=`set_property playlist-pos pos` (全て固定テンプレート、外部由来文字列を property に流さない = RCE 一線・契約 C2)。投入後 mpv playlist を server から編集しない (C1: index と PWA 保持 title が 1:1 固定)。`_STATE_PROPS` に `playlist-pos`/`playlist-count` 追加、`_derive` で `playlist_pos`/`playlist_count` を素通し (`api_video_state` は無改変で透過)。
  - **C. server/app.py = endpoint**: `POST /api/video/play_playlist {url}` (urlguard.normalize → playlist.expand → 空 400 / 展開失敗 502 → play_playlist_load、レスポンス `{titles, count, total}` = C2 の title 供給点) + `POST /api/video/queue/{next,prev}` + `POST /api/video/queue/jump {pos}` (pos は非負 int 検証、bool 除外)。ROUTES に 4 行 (全 Bearer 必須)。**code-reviewer 修正必須 = `import playlist` 欠落 (ハンドラ実行で NameError) を反映**。
  - **D. PWA (web/app.js + index.html + style.css)**: 出口自動判定 = playVideo が `urlHasPlaylist` (URLSearchParams で list= 検出、失敗時正規表現フォールバック) で playPlaylist へ分岐 [URL 欄 1 本維持、ニコニコ mylist 等パス型は対象外]。`#video-queue` = playlist_count>1 で一覧描画 + 現在曲 (playlist_pos) を ▶ 朱赤ハイライト + 前/次 + 曲タップで queue/jump、各操作後に即 poll でハイライト即反映。title は `vCurrentTitle` (vLocalTitle → vQueueTitles[pos] → mpv の順)、vQueueTitles はメモリ + localStorage (`video_queue_titles`、契約 C2、再開復元)。配列長 ≠ playlist-count のとき件数のみ表示・jump 無効化 (C1 を PWA でも担保)。`vExpanding` で flat 展開中 (最大 60s、mpv idle) の pollVideo state 反映を保留し「読み込み失敗」誤判定を抑止。live 混在は `#video-queue-live` で手動「次へ」誘導 (auto-skip タイマー不採用 = §7)。innerHTML 不使用継続、外部 title は textContent 挿入。SW v24→v25。**code-reviewer 修正必須 2 件 (videoStop の vExpanding 未解除 = 展開中キャンセルで 60s 固着 / renderQueue の innerHTML 方針違反) を反映**。
  - **検証 (本番非汚染)**: ①単体 110 件 pass (test_playlist 10 件 = parse/cap/sanitize/single fallback/C4 reject、urlguard 連携を実走)。②headless mpv 0.34.1 (`--vo=null --ao=null` + 別 socket = 本番 `companion-video-mpv.sock` 非接触、`REMOTE_VIDEO_SOCKET` 差替、ローカル mp3 で network 非依存) で play_playlist_load→count=3/pos=0、next/prev/jump の pos 遷移、C1 (count 不変)、stop→idle/count=0 を全 PASS。③Playwright UI スモーク 14 アサーション (web/ を使い捨て http.server 配信 + /api 全モック、本番サーバ/TV/token 非接触) = list= 判定→play_playlist 発火/単一 play 非発火/キュー 3 件描画/「全 5 件中 3 件」件数文言/初期 pos0 ハイライト/title=キュー保持/next・prev・jump の API 発火と body {pos} とハイライト追従/JS エラー 0。
  - **TV 実機検証 (2026-06-18 完了、user 在席)**: 5 曲 YouTube プレイリスト (作業用 BGM、各約 60 分) を投入 → pos0 playing / playlist_count=5。次へ → pos1 pause=false 再生継続。シークで終端へ送り **eof 自動 advance → pos2 へ自動進行・pause=false 再生継続** (中核機能成立)。前へ → pos1、jump(曲タップ相当) → pos4、いずれも pause=false。**PWA 終了+画面ロックで TV 再生継続 (要望3)、開き直しで現在曲 (5 曲目) ハイライト復元 (pos 復元)**。PWA は 5 曲一覧+現在曲ハイライト表示を確認。検証は pixel-6 とは別の一時トークン (label=rv12-smoke-tmp) で state 観測 → 検証後 tokens.json から該当のみ除去 (401 実確認・pixel-6 温存)。`systemctl --user restart` 実施済。
  - **実機で発見・修正した pause バグ**: `play_playlist_load` は先頭 (replace) だけ `pause=no` を付け append には付けていなかった。**実 vo + ytdl 環境では per-file pause 指定の無いエントリが current になると paused で読み込まれ、次へ/自動 advance が各曲で一時停止して止まる** (本番で deterministic に再現: pause=no を持つ entry0 は解決中も再生継続、持たない entry1/2 は pause=true)。headless (vo=null) では再現せず headless 検証ではすり抜けた (テストの穴 = pos 遷移は見たが pause を assert していなかった)。修正 = append にも `pause=no` を付与し全エントリ統一 (reactive に queue_next 後へ set pause を足す案は eof 自動 advance を救えず 2 周目の温床ゆえ不採用)。video.py の follow-up commit (下記)、restart 後 TV で全項目 pause=false を再確認。
  - **台帳同期 (済)**: `workspace/redesign/video-design.md` §0 非目的の「キュー/プレイリスト punt」解除 + C1〜C5 契約を改版履歴に反映済 (workspace repo 09886b2)。bot 側は playlist 非対応のまま (urlguard 無改変ゆえミラー同期不要)。
  - commit = server 8b40e87 / PWA a9a6666 / docs (本コミット)。push は不要 ((C) ローカル git のみ)。
- 2026-06-14 **F-2 発話を撤去 + ホームに「写真」タイルを追加 (user 依頼「発話はもう使わない、削除アンド追加」)**: Phase 4 で voice bot 統合 (renbot) / claude code 側 voice 通知が整い、リモコンからの手動 TTS (F-2) は役割が消えたため一式撤去。代わりに別プロジェクト `~/companion/photos` (Google Takeout 静的フォトギャラリー) への導線をホームに新設。
  - **発話 撤去 (一式)**: UI = `web/index.html` の発話タイル + `#speak` section、`web/app.js` の `sendSay()` / `SCREENS` の "speak" / `say-send` リスナー。server = `server/app.py` の `api_say` / `/api/say` ルート / `_SAY_EXIT_MAP` / `SAY_*` 定数 / `_say_lock` / `import voice`、および未使用化した `import threading` / `import time`。`server/voice.py` をファイルごと削除。設定残骸も撤去 = `.env.example` の `REMOTE_SAY_SH` と `systemd/companion-remote.service` の `Environment=DISPLAY=:0`(say.sh paplay 専用だった、remote プロセスは描画も audio も持たないため不要、daemon-reload + restart 済)。**`~/companion/voice/scripts/say.sh` 本体は無改変** (voice 通知・renbot が使う別軌道、remote とは独立)。`server/dlqueue.py` のコメント「voice.py 同型」を dangling 回避で「say.sh と同じ env 隔離方針」に更新。code-reviewer 指摘 (.env.example/service の F-2 残骸) を反映。
  - **写真タイル 追加**: `data-href` のランチャタイル (▦ 02 / 写真 / Google フォト閲覧)。`initNav` に `[data-href]` → `window.open(url, "_blank", "noopener")` 結線を追加 (user gesture 内、画面遷移なし)。リンク先 = `https://miho-inspiron-3521.tail5e989b.ts.net:8446/` (tailscale serve :8446 → 127.0.0.1:47826、tailscale 境界で保護)。remote サーバ自身は写真を配信しない (proxy しない = 二重配信回避)。ゲームタイルの単純リンク集と同型だが単一行先なので 1 タップ直行。
  - **検証**: 単体テスト全 pass / Playwright UI スモーク (写真タイル click で 8446 への window.open 発火、発話導線消失、既存動画・todo・vault・games 回帰なし) / restart 実施。SW remote-v23→v24。
- 2026-06-12 **RV-11 TVer 再生対応 + 動画画面整理 + 軽量 UI 仕様新設 (user 依頼: TVer 再生対応 / 画面のごちゃつき・実態乖離の修正 / かるい仕様起こし)**:
  - **A. RV-11 検証 (検証が先の手順どおり)**: user からの URL 提供を待たず TVer platform API (匿名 platform_user 作成 → callEpisodeRanking) から実エピソード URL を自力取得。3 本 (今夜、秘密のキッチンで / アメトーーク！ / 水曜日のダウンタウン = 3 系列局) を yt-dlp 2026.03.17 `-J` で解決検証 → **全て DRM 0 / formats 12 / HLS m3u8_native / 無ログイン**。DRM 化・geo/会員制約の懸念は現時点で非該当と確認。
  - **B. allowlist 拡張 (`server/urlguard.py`)**: ALLOWED_HOSTS に `tver.jp` / `www.tver.jp` 追加 (normalize ロジック無改変、ニコニコと同手順)。canonical ベクタ (video-design §4.1) に受理 2 (episodes URL ×2) + 拒否 2 (userinfo 詐称 / suffix 偽装) 追加。bot.py PLAY_ALLOWED_HOSTS ミラー同期 + test_bot.py に同ベクタ追加 (bot repo commit)。remote 90 件 / bot 47 件 全 pass。
  - **C. 事前 DL は不可と裁定 (`normalize_dl` 新設)**: TVer は期限付き見逃し配信で、ローカル保存は配信期限と権利の両面を迂回するため RV-10 経路に流さない (RV-11 注記の判断を確定)。実装 = urlguard に `STREAM_ONLY_HOSTS` (TVer 2 host) + `normalize_dl()` (再生 allowlist から除外判定) を新設、`/api/dl` の門のみ差し替え (再生の門 `normalize` は無改変)。単体 2 件追加 (DL 拒否 / YouTube・ニコニコ DL 継続受理)。bot に DL 経路はないためミラー対象外 (bot.py コメントに明記)。
  - **D. 実弾検証**: loopback = TVer URL の `/api/dl` 400 拒否 / `/api/video/play` 200 → **本番 TV 11s playing** (タイトル・duration 2811s・seekable=True 正常) → シーク +60s 反映 → 停止 idle 復帰。スモーク用トークンは発行→検証→tokens.json から該当 label のみ除去 (401 実確認、pixel-6 無傷)。
  - **E. 動画画面整理 (PWA)**: URL 入力欄が再生用と事前DL用で 2 つあったのを **1 本化** (idle カードの URL 欄 + 「再生」「あとでDL」「クリア」)。事前DL折りたたみは一覧専用 (「事前DLリスト」) に縮退。結果文言は押した場所の近くに出す原則で URL 欄直下 `#video-msg` に集約 (`#video-play-err` を改名、リスト内「再試行」のみ `#dl-result`)。「あとでDL」成功時は欄クリア + リスト自動展開。
  - **F. 実態乖離の文言修正**: placeholder「YouTube の URL」→「YouTube / ニコニコ / TVer の URL」、play 400「YouTube のみ対応」→ 3 サービス併記、DL 400 に「TVer は不可」明記、resolve 目安「最大1分」→「10秒ほど」(vendored hook + force-ipv4 後の実測: YouTube ~10s / ニコニコ 8s / TVer 11s。90s 超の文言エスカレーションは維持)、ホームタイル sub「再生・シーク・音量」→「URL再生・事前DL」、app.py/app.js 内の「40〜70s」コメント残置も更新。
  - **G. 軽量 UI 仕様 = `docs/video-ui.md` 新設**: 画面構成 (排他 3 view + 常設DLリスト + nowbar) / URL 1 本・出口 2 つ / 対応サービス表 (TVer は DL 不可の根拠込み) / 状態機械と真実の所在 / localStorage キー / 変更時の注意 (SW bump・ベクタ同期・restart) を 1 ページに集約。設計大元 (video-design.md / dlqueue-design.md) への参照付き。
  - **検証 (UI)**: SW v22→v23。Playwright (Chromium) で本番ページ読み込み = 新構造 10 アサーション pass + JS エラーなし (使い捨てスクリプト、games の node_modules 借用後撤去)。
  - **台帳同期**: video-design.md §4.1 + 改版履歴 / dlqueue-design.md §2.3 + v1.3 (いずれも workspace repo commit)。restart 実施済 (companion-remote のみ、mpv unit 無改変)。
  - commit (下記)。push は不要 ((C) ローカル git のみ)。

- 2026-06-12 **DL 失敗修正 (-4) + RV-9 ニコニコストリーミング解決 (user 依頼: DL 失敗調査 / ログイン要否 / ストリーミング対応)**:
  - **A. DL 失敗の真因切り分け**: 実運用初日の投入 (nico.ms/sm46423498、15 分動画) が `timeout (3600s)` で failed。切り分け = ①cookies-from-browser firefox で -J 解決成功・720p formats 確認 → ②cookies あり実 DL 90s 計測 = frag 1/154 (~45s/frag = 完走 2 時間ペース) → ③**cookies なしでも同速** = ログイン無関係 → ④`getent ahosts delivery.domand.nicovideo.jp` = **AAAA を返すが ping -6 100% loss / ping -4 12ms** → ⑤`-4` で **3.35MiB/s (90 倍)**。真因 = 網の IPv6 死亡 (RV-8 で実測済) × CDN の AAAA 応答 → yt-dlp が fragment ごとに IPv6 connect timeout を踏む。RV-10 実弾の sm9 1135s も同根 (当初「ニコニコ側帯域制限」と推定したのは誤り、台帳訂正)。
  - **B. 修正 (`server/dlqueue.py`)**: argv に `-4` を追加 (新規オプション 1 つ = 1 周目、RV-8 の「(i) IPv6 fallback 仮説なら force-ipv4 1 周目相当・低リスク」評価と整合)。実弾 = 失敗していた実 URL (タイトル『【超解説】セブンイレブンのあんまんの食べ方』258MB) が **99s で完走**。全 88 テスト pass 維持。本番 failed エントリ #1 は PWA の再試行で復旧可能。
  - **C. ログイン (ticket #18) の裁定 = 不採用**: cookies は速度に無関係を実測確定 (A③)。`--cookies-from-browser` はブラウザ cookie 全体を DL のたびに読む結合 + YouTube への cookie 付与はアカウントリスクがあり、速度問題が `-4` で解決した今は入れる理由がない。**ログイン必須動画 (プレミアム限定等) のニーズが出たら、ニコニコ限定の cookies 渡しを 1 回設計する** (dlqueue-design v1.2 §2.2 に記録)。リモコンからのログイン画面も同理由で不要。
  - **D. RV-9 解決 (`systemd/companion-video-mpv.service` + `vendor/ytdl_hook.lua` 新規)**: §8 手続きの 1 回の設計判断 = (α) 見送り / (β1) mpv 本体更新 / (β2) pre-resolve / (γ) **upstream v0.37.0 hook 無改変 vendored ← 採用** (比較根拠は video-design §8)。sha256 を upstream raw と一致確認 (`4f2e2a2a...`)。unit 変更 = `--ytdl=yes` → `--ytdl=no` + `--scripts=%h/companion/remote/vendor/ytdl_hook.lua`、raw-options に `force-ipv4=` 追加 (B と同根拠の streaming 側適用)。**ファイル名 ytdl_hook.lua は必須** (script-opts prefix がファイル名由来)。
  - **検証**: headless (vo/ao null) = ニコニコ sm9 **8s playing** (旧: resolving 40〜45s → idle 不達) / YouTube VOD 回帰なし (5s playing、seekable=True = live 検出ロジック前提維持)。`daemon-reload` + mpv unit restart (idle 確認後) → **本番 TV で sm9 10s playing** (音量 40 に下げて確認後 stop + 音量 100 復帰)。**3 分 soak** (15 分動画 streaming、code-reviewer 軽微提案 = fragment 取得は mpv/ffmpeg 側で force-ipv4 の射程外のため) = **pos 192s 到達 / stall 0/38 サンプル / cache-duration 430s / underrun なし** — fragment 側の IPv6 stall も出ず。
  - **known-good ペア台帳更新 (§8)**: **mpv 0.34.1 core + vendored ytdl_hook v0.37.0 + yt-dlp 2026.03.17 + 検証日 2026-06-12 + 結果: ニコニコ/YouTube streaming + DL 経路 すべて pass**。撤去条件 = mpv 本体を 0.37+ に更新したら vendored hook と `--ytdl=no`/`--scripts=` を撤去して組み込み hook に戻す (hook×core のバージョン非対称は yt-dlp 更新時の検証面が 1 つ増えるため恒久構成にしない)。
  - **code-reviewer**: 修正必須なし。軽微 3 件 = bot 側 load 失敗の logging (反映、bot repo) / soak 検証 (実施、上記) / vendor/ の commit 漏れ注意 (本 commit に含めた)。
  - **PWA 文言の注意**: DL 追加・play 拒否の文言は「YouTube / ニコニコのみ対応」のまま整合 (変更なし)。restart は companion-remote (B の反映、実施済) + companion-video-mpv (D の反映、実施済) の両方完了。
  - commit (下記)。push は不要 ((C) ローカル git のみ)。

- 2026-06-12 **RV-10 事前ダウンロードキュー (F-dl) 実装完了 (「RV-10 やって」名指し着手)**: 外出先から PWA で URL をキュー投入 → 自宅機で yt-dlp が 720p 上限でローカル保存 → 帰宅後「事前DL」リストからローカル即再生 (yt-dlp 解決 40〜70s が消える = 待ち時間短縮の本命)。
  - **設計ラウンド 1 回 (TODO 記載どおり)**: `~/companion/workspace/redesign/dlqueue-design.md` v1.1 が成果物。v1.0 ドラフト → devil subagent レビュー (指摘 17 件 = 致命 1 / 要修正 7 / 軽微 9) → 反映して確定。主要裁定: worker = server 内 daemon thread 1 本 (新 unit なし、単一 DL lane) / 容量 20 GiB は enqueue 時 + worker 着手直前の**二重ガード** (致命指摘 = 19GiB 時の N 件連続投入で際限なく積み上がる穴を塞ぐ) / restart 中断は failed 1 回確定 (自動再開しない、回復は user 再試行) / 帯域競合 (DL × mpv 再生) は**受容を 1 回確定** (再生検知→DL 抑止の cross-control は 2 周目の温床、`--limit-rate` は観測前の推測数値ゆえ不採用。視聴中 stall 悪化が実観測されたらその時 1 回だけ確定) / 自動掃除 evict 不採用 (掃除は PWA 削除ボタンのみ)。
  - **A. データ層 + worker (`server/dlqueue.py` 新規)**: state = `.state/dlqueue.json` + `dlqueue.lock` flock (tickets.py 同型、lock fd は毎回 open = スレッド間排他の契約)。**_save のみ atomic write (os.replace) に意図的逸脱** — JSON 破損 → 空復元で DL 済み実体 (重い) が UI から削除不能になる事故を破損自体を塞いで防ぐ (devil 対案の「state に無い dl-* を起動時削除」は state 破損 → 実体全削除の逆事故ゆえ不採用、設計 §1.3)。worker は `threading.Event` 起床 (ポーリングなし) + 起動直後 1 回 set で残置 queued を drain + 項目単位 try/except でループ生存 (worker 死 = enqueue 200 + 永久 queued の無音破綻を防ぐ)。yt-dlp 直叩きは env 隔離 (voice.py 同型、HOME/PATH のみ) + `--ignore-config --no-playlist` + mpv unit と同一 format 文字列 + `start_new_session` + timeout 3600s 時 `os.killpg` (merge 中 ffmpeg の孤児化防止)。**filepath は stdout に依存せず rc==0 後の FS glob `dl-<id>.*` (.part 除外) で 1 回確定**、stdout (`--print after_move:title`) は title 専用 (制御文字 strip + 200 字 cap)。failed 確定時のファイル掃除は経路 (rc≠0/timeout/中断/内部例外) を問わず一般規則。dedup = 同一 URL の queued/downloading があれば既存項目を返す。
  - **B. 起動シーケンス (`server/app.py` main)**: `bind 成功 (単一インスタンス保証) → dlqueue.recover() (downloading 残骸を failed 化 + 掃除) → start_worker()` の順序固定 (bind 前 recovery だと 2 つ目のプロセスが稼働中 DL を壊す)。
  - **C. API 4 ルート (Bearer 必須)**: `POST /api/dl {url}` (urlguard.normalize 必須 = F-video play と同一の門、容量上限 507) / `GET /api/dl` (id 降順 + usage/limit) / `POST /api/dl/delete {id}` (downloading は 409、エントリ + 実ファイル削除) / `POST /api/video/play_local {id}` — **HTTP から path を一切受けない (int id のみ)**。id → done 項目の basename → realpath + commonpath で downloads/ 境界確認 (vault 同型、state 改竄・symlink 脱出 reject) → `video.play(<abs path>)`。**verb whitelist 無改変**、ローカル絶対 path は ytdl_hook 非経由。
  - **D. 通知 (bot.py 無改変)**: DL 完了/失敗時に `$XDG_RUNTIME_DIR/companion-bot.sock` (bot 既存 notify socket) へ平文 1 ショット (`[dl] 完了: <title>`)。**state 確定 (flock 下) → flock 解放 → settimeout(5) 送信**の順序契約 (flock 保持のまま送ると bot 無応答で全 endpoint ハング)。失敗は握りつぶし (ベストエフォート、真実は PWA リスト)。
  - **E. PWA**: video 画面内に折りたたみ「事前DL」(todo 完了履歴と同型の同居判断、別タイルにしない)。専用 URL 欄 (即時再生と取り違えない) + リスト (待機中/DL中/済=再生/失敗=error 表示+再試行=同 URL 再 enqueue) + 削除 + 使用量表示。開いている間のみ 15s ポーリング。**play_local 中は mpv media-title がファイル名 (dl-7.mp4) になるため PWA 側で queue の title に表示上書き** (force-media-title 案は verb whitelist 変更ゆえ不採用)。createElement のみ継続。SW CACHE **remote-v21→v22**。
  - **テスト**: `tests/test_dlqueue.py` 28 件追加 (採番/dedup/容量 507/遷移/recovery 掃除/delete 409/play_local 境界 = 改竄・symlink・絶対 path reject/fake yt-dlp での worker 成否/title サニタイズ)。既存 60 + 28 = **全 88 件 PASS**。loopback HTTP 往復は隔離 temp state + fake yt-dlp + 別ポートで **11 項目実観測 PASS** (401/400/200 enqueue/dedup/worker done/usage/play_local 404・503/delete とファイル消滅)。本番 .state 非接触。
  - **実弾スモーク (実 yt-dlp 2026.03.17 + 実ネットワーク、隔離 dir)**: ① YouTube 短尺 (jNQXAC9IVRw) **47s で DL 成功** (title='Me at the zoo' 取得 OK)。② **ニコニコ sm9 = 1135s (約 19 分、28.4 MB) で DL 成功** — domand の cookies を yt-dlp 自身が処理し、**RV-9 (mpv ytdl_hook cookies 未対応 → HLS 403) を DL 経路が迂回することを実証** (本設計の採用理由を実弾確認)。③ 両ファイルとも headless mpv (`--vo=null --ao=null` + 私設 socket、**TV 非接触 = user 就寝中のため本番 mpv unit に触らない**) で play_local 経路 → **playing 到達確認**。media-title がファイル名になる実挙動も確認 (PWA title 上書きの妥当性裏取り)。
  - **known-good 台帳 (§8 整合)**: DL 経路も mpv unit と同一 yt-dlp 2026.03.17 (`/home/miho/bin/yt-dlp`)・同一 format 文字列。`-U` 禁止継続。破綻シグナル「全 DL failed」→ reactive 手動差替は §8 と同一運用。**ffmpeg /usr/bin (4.4.2) が merge の新規 load-bearing 前提** (存在確認済、設計 §2.1 に記録)。ニコニコ DL 実測 19 分/5 分動画はニコニコ側帯域制限とみられる (「投入して放置」運用なら許容)。
  - **code-reviewer**: 修正必須 1 件 (worker loop の例外防護が _process のみで _claim_next/_finish が裸 = state 層 OSError 1 発で thread 死) → 反映済み。軽微 2 件 = 容量ガード flock 外の契約コメント (反映) / fmtBytes の GB 表記 (表示のみ、見送り)。
  - **restart 実施済** (`systemctl --user restart companion-remote.service`、mpv unit 非接触)。**未実施 (user 委譲)**: Pixel-6 実機での「事前DL」UI 目視 + 本番 TV での play_local 確認 (就寝中につき TV を光らせない判断) + Telegram 通知の実弾確認 (本番 bot socket への送信は通知音が鳴るため就寝中は見送り)。
  - commit (下記)。push は未実施 (user 実施)。

- 2026-06-11 **ニコニコ動画対応 = URL allowlist 拡張 (user 依頼 2026-06-11、orc 経由)。allowlist + テスト + 台帳まで完了、mpv 実再生は失敗 → RV-9 へ分離**:
  - **A. allowlist (`server/urlguard.py`)**: ALLOWED_HOSTS に `nicovideo.jp` / `www.nicovideo.jp` / `sp.nicovideo.jp` / `nico.ms` の 4 host を追加。**`normalize()` ロジック本体は無改変**(hostname 照合のみ・path 非分岐の方針維持、frozenset への host 追加と docstring 更新のみ)。`embed.nicovideo.jp` はユーザーが貼る共有 URL でないため除外(攻撃面を増やさない)。`live.nicovideo.jp`(ニコ生)はスコープ外(将来候補、上記 TODO に 1 行残置)。
  - **B. テスト (`tests/test_urlguard.py`)**: canonical ベクタ(video-design §4.1)に受理 4 件(`www.nicovideo.jp/watch/sm9` / `nico.ms/sm9` / `sp.nicovideo.jp/watch/sm9` / `nicovideo.jp/watch/sm9`)+ 拒否 4 件(userinfo 詐称 `evil@nicovideo.jp` / suffix 偽装 `nicovideo.jp.evil.com`・`nico.ms.evil.com` / 非 allowlist サブドメイン `embed.nicovideo.jp`)を追加。**全 60 件 PASS**(YouTube 既存受理/拒否の退行なし)。bot.py ミラー(`PLAY_ALLOWED_HOSTS` + tests/test_bot.py、bot repo 側 commit)も同期済、video-design.md §4.1(一元定義)にも同日反映。
  - **設計判断**: video-design §5 の「security + architect 再 cross-review」は、host 4 件追加・ロジック非変更の小粒拡張のため **code-reviewer の security 観点レビューで代替**(orc 判断)。
  - **known-good 実測**: yt-dlp 2026.03.17(凍結版、`/home/miho/bin/yt-dlp`)NiconicoIE で `https://www.nicovideo.jp/watch/sm9` -J 解決 57s / `https://nico.ms/sm9` 42s(generic redirect 経由、いずれも orc 実測・ログインなし)。implementer 再現 = ytdl_hook と同一引数で rc=0 / 36.8s / JSON 1.06MB。
  - **mpv 実再生スモーク = 失敗(2 回再現)**: `systemctl --user restart companion-remote.service` 後(新 urlguard 反映、/api/health 200)、`POST /api/video/play`(sm9)→ resolving 40〜45s → **idle に戻り playing 不達**。切り分け: hook と同一引数の yt-dlp 直叩きは成功 → 失敗は mpv 側。真因 = domand 配信は per-format **`cookies` フィールド(domand_bid)** を要求するが Cookie は `http_headers` に乗らず、**mpv 0.34.1 ytdl_hook は `cookies` フィールド未対応** → HLS playlist 取得 403。実証 = m3u8 を cookie なし GET=**403** / cookies 値を Cookie ヘッダ付与 GET=**200**。journal(companion-video-mpv)は `Cannot load libcuda.so.1` のみ(yt-dlp/hook の失敗詳細は journal に乗らない既知設計)。対症療法は打たず **RV-9 として TODO に分離、設計判断待ち**。
  - **検証後始末**: 検証用 token(label=niconico-smoke-tmp)は発行→検証後 tokens.json から該当のみ削除(実機 token `pixel-6` 温存・平文非残置)、削除後 401 を実観測。companion-video-mpv は無改変につき再起動なし。
- 2026-06-09 **やること画面に完了履歴の折りたたみ一覧を追加 (ticket #3、orc 経由)**: done にしたチケットは一覧から消えるだけで履歴閲覧 UI が無かった。後で「何を完了したか」を振り返れる**閲覧専用**の折りたたみ一覧をやること画面に追加。
  - **A. データ層 (`server/tickets.py`)**: `history()` を追加。done ステータスのみを **`updated` 降順(新しい完了が先頭)** で `{"tickets": [...]}` 形で返す(counts は不要なので付けない)。既存 `active()` / `all_tickets()` の `_locked()` + `_load()` パターンに揃え、CLI は無改変(`list --all` で従来どおり done が見える)。
  - **B. エンドポイント (`server/app.py`)**: `GET /api/todo/history`(**Bearer 必須**)を追加し `tickets.history()` を返す。ROUTES 登録は既存 `/api/todo` 系の流儀。既存 `/api/todo` / `/api/todo` POST / `/api/todo/status` は無改変。
  - **C. フロント (`web/index.html` + `app.js` + `style.css`)**: 一覧(`#todo-list`)カードの下に折りたたみセクション(トグルボタン + `#todo-history-list`)を追加。**別画面・別ホームタイルにはしない**(履歴は主要動線でないため todo 画面内に同居)。デフォルト畳む。`renderHistory()` / `refreshHistory()` を追加し、**履歴セクションを開いた時 / todo 画面を開いた時(開いていれば)** に `/api/todo/history` を取得描画(**active 一覧の 15s ポーリングには乗せない** = 履歴は頻繁に変わらず無駄ポーリングを増やさない)。各 done 項目に `#id` / by マーク(既存 `TODO_BY_MARK`)/ text / **完了日時**(`updated` epoch を `fmtDateTime` ヘルパで `YYYY-MM-DD HH:MM` 整形)。**完了ボタンも done→todo 復活機能も出さない=閲覧専用**(YAGNI)。空は「(完了履歴なし)」。描画は `createElement` のみ(innerHTML 不使用継続)。done 項目は `.todo-item` 流用 + grey 系で**トーンダウンして active と区別**(`.todo-history-*` 命名)。
  - **SW**: app shell(index.html / app.js / style.css)更新につき `CACHE` を **remote-v18→v19** bump。SHELL は新規ファイル無しで不変。`/api/todo/history` は既存 `/api/` network-only でキャッシュ素通し。
  - **テスト**: `tests/test_tickets.py` に `history()` 検証 3 件追加(done のみ返す / `updated` 降順 / done 0 件で空 dict)。既存 57 + 3 = **全 60 件 PASS**。loopback HTTP 往復は**隔離した temp state 上で別ポート起動**して実観測(401 無トークン / 200 で done のみ `updated` 降順 / `/api/todo` が done を含まない)。本番常駐サーバは restart 禁止ゆえ旧コードのままで、**本番 `.state/tickets.json`(既存 done 2 件)は非接触**。
  - **restart 未実施**: 反映には `systemctl --user restart companion-remote.service`(server/*.py メモリ常駐 + SW 版更新)。commit 87f697e。
- 2026-06-08 **バージョン表記を追加 (デプロイ版を一目で確認、orc 経由)**: OWNER が「今スマホに乗っているのが最新か」を確認できるよう、ホーム masthead にデプロイ中の版(git short hash + コミット日)を表示。直近の SW キャッシュ/画像表示変更で版ずれ確認のニーズが出たため。**ビルド工程なし(vendored 資産 + stdlib http.server)で自動更新される版**にした。
  - **A. サーバ版解決 (`server/version.py` 新規)**: `REPO_ROOT`(remote repo ルート)に対し `git rev-parse --short HEAD` と `git show -s --format=%cs HEAD`(YYYY-MM-DD)を **subprocess で起動時に1回だけ**実行し `APP_VERSION` を確定。git 不在/非 repo/非ゼロ終了/timeout は `None` → `"unknown"` フォールバック(例外で起動を止めない。成否は1回で確定、リトライ/stderr 文言マッチ/分岐の積み増しなし=`~/companion/CLAUDE.md` 設計上限ルール準拠)。**版文字列の生成は git に依存しない純関数 `format_version(short_hash, date)` に切り出し**(hash+date→`"<hash> (<date>)"` / hash のみ→`"<hash>"` / hash なし→`"unknown"`、前後空白除去)、これを単体テスト。
  - **B. エンドポイント (`server/app.py`)**: `GET /api/version`(**Bearer 必須**)を追加し `{"version": version.APP_VERSION}` を返す。ROUTES 登録・auth 適用は既存 vault route と同じ流儀。`/api/health` は無認証で情報を絞る既存方針を維持するため、版は **authed 側に配置**(home は token 設定済みでのみ版を取りに行く)。ハンドラは定数を返すだけ(リクエスト毎に git を再実行しない)。
  - **C. フロント (`web/index.html` + `app.js`)**: 既存の masthead `<div class="ver">Remote</div>` に `id="app-version"` を付与(新規 DOM 追加なし、`style.css` の `.ver` 控えめトーンを流用)。`refreshVersion()` を追加し、認証後(token 保存時 / 起動時 / token 設定済み)に `api("/api/version")` で取得して `"v<hash> (<date>)"` を表示。トークン未設定/取得失敗は `"Remote"` に戻して握り潰す(リトライループなし)。DOM は既存規律どおり `textContent` のみ(innerHTML 不使用)。
  - **SW**: app shell(index.html / app.js)更新につき `CACHE` を **remote-v16→v17** bump。SHELL は新規ファイル無しで不変。`/api/` は既存 network-only でキャッシュ素通し(`/api/version` も stale 化しない)。
  - **テスト**: `tests/test_version.py` 5 件追加(hash+date / hash のみ / hash なし→unknown / 空白除去 / `APP_VERSION` が非空文字列)。既存 52 + 5 = **全 57 件 PASS**。`py_compile`(version.py / app.py)+ `node --check`(app.js / sw.js)OK。`version.resolve()` を実 repo に対し実行 → `9b60fe9 (2026-06-08)` を確認。
  - **設計判断**: 版を authed 側(`/api/version`)に置いた(無認証 health は情報を絞る既存方針に最も寄る案。home はトークン設定済みでのみ版を表示するため authed で支障なし)。
  - **restart 未実施**: `server/*.py` 編集はメモリ常駐で `systemctl --user restart companion-remote.service` まで無反映(`/api/version` が 404 のままになる)。restart はメイン機ユーザー操作のため未実施(`web/` 静的は即反映で乖離する点に注意)。Pixel-6 実機での版表示目視は orc/user 委譲。SW v17 は次回アクセス時 skipWaiting で更新。
  - commit (下記)。push は未実施(user 実施)。

- 2026-06-08 **F-vault ノート内画像の表示を追加 (画像配信エンドポイント + CSP 緩和 + ローカル画像 blob 表示、orc 経由)**: ノート閲覧で画像が出ない問題を修正。原因は 2 つ — (1) `index.html` の CSP が `img-src` 未指定で `default-src 'self'` にフォールバックし、clips ノートのリモート画像(`https://pbs.twimg.com/...`)が全滅。(2) サーバ `vault.py` は `.md` しか配信せずローカル保存画像の取得経路がない。
  - **A. サーバ画像配信 (`server/vault.py` + `app.py`)**: `vault.py` に `get_image(rel_path)` を追加。既存 `_safe_abspath` の realpath+commonpath 境界判定を `_resolve_in_vault` に**共通化**(traversal/範囲外/symlink 脱出を 1 回確定する核は .md/画像で共有、拡張子の許否のみ呼び出し側が決める。条件分岐を積まない=設計上限ルール準拠)。画像版は拡張子 allowlist `IMAGE_TYPES`(`.jpg/.jpeg/.png/.gif/.webp` → content-type)に限定、**read 専用**(`open("rb")` のみ、書き込み/mkdir なし)。`app.py` に `GET /api/vault/image?path=`(**Bearer 必須**)を追加、`VaultError`→403(範囲外/不正/非画像)/404(not found)写像は `api_vault_get` と一致。バイナリ送出は `_Binary` ラッパを 1 回型判定し、icon png(STATIC)と同じ `_send` のバイナリ対応を再利用(JSON 経路と分岐 1 箇所のみ)。
  - **B. フロント表示 (`web/app.js`)**: ①リモート URL 画像 `![](https://pbs.twimg.com/...)` は marked が `<img src>` を生成 → CSP 緩和で表示(JS 追加なし)。②ローカル埋め込み `![[name.(jpg|jpeg|png|gif|webp)]]` は `vaultRewriteWikilinks` を拡張して `<img data-vault-img="name">` に前処理(画像埋め込みを通常 `[[...]]` より先に置換、非画像埋め込み・通常リンクは挙動不変。属性値は既存同様エスケープ)。`DOMPurify` の `ADD_ATTR` に `data-vault-img` 追加。描画後に `img[data-vault-img]` を走査し **Bearer 付き `api()`** で `/api/vault/image?path=attachments/<name>` を **blob fetch → `URL.createObjectURL` → `img.src`**(`<img src>` は Authorization を送れないため必ず blob 方式、生 src をブラウザに読ませない)。契約: bot 側は画像を vault `attachments/<basename>` 保存・本文で `![[<basename>]]` 参照、フロントは `attachments/` 固定で解釈(Obsidian `attachmentFolderPath="attachments"` と一致)。**object URL リーク防止**: ノート再描画(`vaultRenderMarkdown`)の冒頭で前回 URL を `revokeObjectURL`。取得失敗は壊れアイコンを残さずプレースホルダ alt で握り潰す(リトライループなし)。
  - **C. CSP 緩和 (`web/index.html`)**: `default-src 'self'; img-src 'self' data: blob: https://pbs.twimg.com; object-src 'none'; base-uri 'none'`。`blob:`=ローカル画像 object URL / `data:`=軽量プレースホルダ / `https://pbs.twimg.com`=clips のリモート画像(host 限定、ワイルドカード `https:` にしない)。inline 不許可・`object-src`/`base-uri` は維持。
  - **SW**: `/api/` は既存 fetch handler で network-only(`startsWith("/api/")` で return)のため `/api/vault/image` はキャッシュ素通し・stale 化しない(変更不要)。app shell 更新につき `CACHE` を **remote-v14→v15** bump。SHELL は新規ファイル無しで不変。
  - **テスト**: `tests/test_vault.py` に `get_image` のパス検証 **10 件追加**(正常 png/jpg の content-type + バイト先頭 / 非画像拡張子(.exe)拒否 / .md は画像 EP から不可 / 不在 404 / traversal `../`・絶対 path・null byte・symlink 脱出・空/非 string 拒否)。既存 42 件 + 10 = **全 52 件 PASS**。`py_compile`(vault.py/app.py) + `node --check`(app.js/sw.js) OK。
  - **設計判断**: バイナリ応答を既存 `(code, obj)` 契約に載せるため `_Binary` ラッパ + dispatch での 1 回型判定を採用(handler が自前送信して契約を崩す案より、既存 `_send` 再利用・ルートテーブル一貫の方が既存慣習に寄る)。
  - **restart 未実施**: `server/*.py` 編集はメモリ常駐で `systemctl --user restart companion-remote.service` まで無反映。restart はメイン機ユーザー操作のため未実施(`web/` 静的は即反映で乖離する点に注意)。Pixel-6 実機での画像表示目視は orc/user 委譲(token 平文を claude 側で扱わない方針)。SW v15 は次回アクセス時 skipWaiting で更新。
  - commit `06eddaa`。push は未実施(user 実施)。

- 2026-06-03 **vault 閲覧機能を追加 (F-vault、read-only、orc 経由)**: 出先から Pixel-6 で `~/companion/vault/` 配下の全 .md を**閲覧専用**で読む。tickets(F-todo)と同じ型(server module + 明示ルート Bearer 必須 + PWA タイル/詳細画面)。**vault へは read のみ・書き込み/編集 UI は一切作らない**。
  - **サーバ `server/vault.py`(read-only データ層)**: `VAULT_ROOT` を `os.path.realpath` で正規化して canonical root を 1 つ固定、env `REMOTE_VAULT_ROOT` で上書き可(既定 `~/companion/vault`)。3 API はすべて GET / Bearer 必須(`app.py` ROUTES に 3 行 + 静的 allowlist に vendored ライブラリ 2 件)。
    - `GET /api/vault/list` — `os.walk` で全 .md を再帰列挙しフォルダ別グルーピング(`{root,count,folders:[{folder,notes:[{path,name}]}]}`)。ドットディレクトリ(.obsidian/.git/.claude 等)を `dirnames[:]` in-place 除外で walk から落とす。
    - `GET /api/vault/get?path=` — 1 ファイルの生 markdown。**パストラバーサル対策 = `_safe_abspath`**: 要求 path を root と join → `os.path.realpath` で `..`/symlink を畳む → `os.path.commonpath([root,candidate]) == root`(prefix 文字列一致でなく境界で照合、`vault-evil` 兄弟誤認を回避)+ 拡張子 `.md` を検証してから open。外れたら `VaultError`。null byte / 絶対 path / 非 string も拒否。open は `"r"` のみ(書き込み/削除/mkdir なし)。
    - `GET /api/vault/search?q=` — **採用**(stdlib のみで安価)。全 .md 横断のファイル名 + 本文部分一致(大小無視)、ヒット箇所前後 40 字 snippet、結果 100 件で打ち切り。
    - 400/403/404 切り分けは構造で確定: `VaultError` のうち `"not found"` のみ 404、他(traversal/範囲外/非.md)はすべて 403(stderr/文言マッチで分岐を増やさない)。
  - **PWA(web/)**: ホームに「¶ 06 ノート」タイル + `<section id="vault">` 追加(tile-count 5→6、`SCREENS` に "vault")。一覧ビュー(検索 + フォルダ別リスト)→ タップで本文ビュー。本文ビュー内の「‹ 戻る」は一覧へ、一覧では(`data-home`)ホームへ(capture-phase で出し分け)。
    - **markdown 描画方式 = クライアント側 vendored ライブラリ**(stdlib 縛り維持・サーバで HTML 変換しない): `marked@12.0.2`(35 KB, parse) + `dompurify@3.0.11`(21 KB, sanitize)を `web/` に同梱、CSP `default-src 'self'` を満たす同一オリジン script として `app.js` 前に読み込み。**XSS 対策**: 本文は owner 自身の信頼できる vault 由来だが、レンダラ素通しを防ぐため `marked.parse` → `DOMPurify.sanitize`(style/form/input 系 FORBID、onerror/onload FORBID)を通してから `innerHTML`。app.js は本来 innerHTML を使わない規律のため、本文描画を唯一の例外として理由をコードコメントに明記。
    - **wikilink 解決**: `[[target]]` / `[[target|alias]]` を marked 前処理で `<a data-vault-link>` に置換(属性値は自前エスケープ)、DOMPurify に `data-vault-link` を ADD_ATTR 許可、クリックで同ビューア内ジャンプ。target→実 path の解決辞書は `list` 取得時に同時構築(追加フェッチ不要、name/path/拡張子なし path をキー化)。解決不能リンクは遷移せずメッセージのみ。
    - **frontmatter**: 先頭 `---...---` を本文から分離し、key/value をメタ情報として簡潔表示(本文には出さない)。
    - **SW**: `marked.min.js`/`purify.min.js` を SHELL precache に追加、`CACHE` を **remote-v12→v13** bump。manifest 変更なし。
  - **テスト**: `tests/test_vault.py` 19 件追加(list が全 .md 列挙 / ドットディレクトリ除外 / フォルダ別グルーピング / 既知ファイル get / 不在 404 / **パストラバーサル拒否 = ../・絶対 path・非.md・null byte・symlink 脱出・`root-evil` 兄弟 prefix 誤認** / search 本文・ファイル名・大小無視ヒット)。既存 23 件と合わせ **全 42 件 PASS**。
  - **動作確認**: `systemctl --user restart companion-remote.service` 後 loopback curl 往復 実観測 PASS — 無認証 list=401 / list count=82(vault の .md 全件、.claude/.obsidian/.git 除外と一致) / 既知 README.md get / `../../etc/passwd.md`=403 / `../remote/server/app.py`=403 / `game_state.json`(非.md)=403 / 不在=404 / search "tailscale"=1 件 / `/marked.min.js`・`/purify.min.js`=200。検証用 token は発行→検証後 tokens.json から該当のみ削除(実機 token `pixel-6` 温存・平文非残置)、その後 restart で無効化確認。Pixel-6 実機目視は user 委譲。
  - commit (下記)。push は未実施(user 実施)。

- 2026-06-03 **リモコン UI を「ホーム型タイルランチャー + ミニマルデザイン」に全面リデザイン (ticket #2, orc 経由)**: フラット縦積み(動画/発話/OS/やること/ゲームを 1 列に積む)でゴチャついた画面を、情報設計から作り直し。**機能は 1 つも削らず、サーバ API は無変更、見た目とナビゲーション構造のみ刷新**。
  - **選定プロセス(user 反復選択)**: ①構造 3 案(下タブ/ホーム/集約スクロール)を提示 → user「ホーム型」選定。②見た目を 0 から並列デザイナー subagent 4 人で発散(温かみ/ネオングラス/ミニマル/家電スキューモーフィック) → user「ミニマル(タイポ主導・罫線グリッド)」選定。③配色 4 種(朱/琥珀ダーク/紺&真鍮/深緑) → user「紺&真鍮(生成り地+濃紺、目に優しい)」の地を選定。④アクセント赤 3 種(朱赤/真紅/ワイン) → user「朱赤 #d8392a」確定。**選定は使い捨て mock を本番 server の STATIC に一時追加 → tailscale serve 経由で Pixel-6 実機プレビューさせて実施**(ASCII では判断不可の user feedback を受けた方式転換)。
  - **確定デザイン**: 地=生成り `#f4f2ea` / テキスト・罫線=濃紺 `#1b2a3a` / アクセント=朱赤 `#d8392a` / `--line:#ddd9cd`。ミニマル/エディトリアル骨格(2 列罫線グリッド・特大タイポ見出し・ナンバリング ▶01 等・uppercase ラベル)。**ホーム**(masthead + glance[接続ドット+disk/mem/温度の要約] + 再生中バー[再生中のみ表示・タップで動画詳細へ] + タイル 5 枚) → タイルタップで**各機能の詳細画面**(左上「‹ 戻る」)、`SCREENS` 配列 + `classList.toggle("active")` の素直な画面切替。
  - **移植方針(状態機械を壊さない)**: 現行 `app.js` の API 呼び出し・動画状態機械(IDLE→RESOLVING→PLAYING⇄PAUSED/LIVE)・ポーリング・localStorage キー・innerHTML 不使用(createElement/textContent)は**全面流用**、DOM 結線を新構造へ。glance を要約専門化 + `renderNow` を再生中バーへ昇格、todo バッジをホームの「やること」タイルへ、games/todo の折りたたみカードは廃止し詳細画面化。**CSP 厳守**(mock の inline CSS/JS を `style.css`/`app.js` に分離、index は外部参照のみ)。
  - **機能保全(5 機能、code-reviewer 確認で欠落ゼロ)**: 動画(状態機械/LIVE ●/close≠stop の可逆・不可逆/±N スキップ+ステップ可変/音量/取りこぼし・失敗文言/reopen) ・ 発話(speaker/409・429・503 文言) ・ OS status(glance 要約+詳細 dl) ・ やること(未対応バッジ/追加/完了/起票者タグ 🙋🤖) ・ ゲーム(GAMES 配列・rel=noopener)。「閉じる」は可逆セマンティクス維持しつつホームへ戻す挙動を追加。
  - **将来枠(今回は非表示=設計判断)**: vault 閲覧(#1)・done 履歴(#3) は未実装ゆえタイル/リンクを**出さない**(押せないものを出さない)。`.grid` にタイル 1 枚 + section 1 つ追加で足せる構造にし、拡張点を index.html にコメント明示。
  - **SW**: web アセット更新につき `CACHE` を **remote-v11→v12** bump(SHELL は新規ファイル無しで不変)。選定用 mock(`mock.html`/`mock1`〜`4.html`) と app.py の一時 STATIC エントリ(**未コミットだったため削除で HEAD と一致 = app.py 無変更**)、使い捨て検証スクリプトは撤去済み。
  - **検証**: Playwright/Chromium(390x844, 本番ポート 47824) で **36 アサーション ALL PASS**(token 未設定→token-setup → ダミー注入→ホーム → 5 タイル各々詳細遷移→戻る / 朱赤 `--acc=#d8392a`・紺地 CSS 適用 / 各画面 横はみ出しなし / pageerror 0 / 動画 resolving・transport[VOD は seekrow+skiprow]・LIVE[badge 表示・seek/skip 非表示] の各 view / 再生中バー表示+タップ遷移)。検証用一時 token は発行→検証後に tokens.json から該当のみ削除(実機 token `pixel-6` 温存・平文非残置)。**server restart 実施済**(`/` 200・`/mock` 404 で mock 撤去確認)。**code-reviewer 修正必須なし**(軽微提案 1 = `goHome()` 配置は動作正常ゆえ見送り)。
  - **未実施(user 委譲)**: Pixel-6 実機での新 UI 目視・操作確認(token 平文を claude 側で扱わない方針)。SW v12 は次回アクセス時 skipWaiting で更新。
  - commit `178a9d9`。push は未実施(user 実施)。

- 2026-06-03 **共用 TODO/inbox(チケット)機能を追加 (F-todo、v1-α 系列 = bot.py 非依存、user 要望)**: user と AI(claude セッション) 共用の inbox。user は PWA で起票 → 番号 `#N` が振られ「#N やって」とセッションで AI に渡せる。AI は稼働中に思いついたら CLI で起票し、user は件数バッジで気づく。「Tailscale 通したときだけ見える」要件は既存リモコン(tailscale serve + Bearer token + PWA shell)への相乗りでゼロコスト充足。
  - **実体 = single source of truth `.state/tickets.json`**(0o600, git 外、tokens.json と同居)。`server/tickets.py` がデータ層 + CLI。server(常駐 ThreadingHTTPServer)と claude CLI(別プロセス)が競合して書くため `.state/tickets.lock` の `fcntl.flock(LOCK_EX)` で read-modify-write 全体を囲み、採番(next_id)を flock 内で 1 回引いて確定(`~/companion/CLAUDE.md`「条件分岐は state 側で 1 回確定」準拠、対症療法 2 周目を打たない)。`_load` は壊れ JSON を空正規形に復元 + next_id を max(id)+1 で復元。id は連番・再利用しない。
  - **API**(`server/app.py` ROUTES に 3 行、全て Bearer 必須): GET `/api/todo`(done 除く一覧 + counts) / POST `/api/todo`{text}(起票 by=user) / POST `/api/todo/status`{id,status}(状態変更)。400/404 切り分けは「id/status を先に検証 → 残る TicketError = 該当なし = 404」と構造で確定(stderr/文言マッチで分岐しない)。
  - **状態 3 段階** todo/doing/done。doing は主に AI が `start` で立てて進捗可視化。done は active 一覧から外れ tickets.json に履歴として残る(完了は一覧から消える + 履歴保持、user 確定仕様)。
  - **PWA**(index.html/app.js/style.css): 折りたたみ「やること」カード(ゲームカードと同形のトグル click + Enter/Space、既定畳む)。見出しに未対応(todo+doing)件数バッジを常時表示、各行に番号 `#N` + 起票者タグ(🙋user / 🤖ai) + 完了ボタン。15s ポーリングでバッジ更新(展開中は一覧も)。`api()` ヘルパ流用、innerHTML 不使用継続。**SW CACHE remote-v10→v11 bump**。
  - **CLI**(claude セッション用): `python3 server/tickets.py add "text" [--by ai]`(既定 user) / `list [--all]` / `show <id>` / `start <id>` / `done <id>`。
  - **検証**: 単体 16 テスト追加(採番連番 / by・status デフォルト / 遷移 / done 除外 / 不正 status 拒否 / counts / 並行 add 30 スレッド衝突なし)+ 既存 urlguard 7 と合わせ **23 全 pass**。server `systemctl --user restart` 後に loopback curl で 3 ルート 401(再起動忘れ 404 でないこと確認)→ 一時 token で認証込み往復(add → doing → done で active から消える / bad status 400 / missing id 404 / empty text 400)を**実観測 PASS**。CLI 往復も観測。code-reviewer 修正必須なし。検証用データはクリーンアップ済(一時 token 破棄・pixel-6 温存・tickets.json は空から運用開始)。
  - **運用フロー**: user が PWA 起票 → `#N` → セッションで「#N やって」 → AI が `tickets.py show N` で詳細 → `start N`(着手中) → 作業 → `done N`。逆に AI が `tickets.py add --by ai "…"` で起票 → user はバッジで気づく。
  - **未実施(実機 / user)**: Pixel-6 実機での PWA 起票 → 一覧 / バッジ → 完了 UI 確認(token 平文を claude 側で扱わない設計に沿い user に委ねる、RA-7 と同方針)。SW v11 は次回アクセス時 skipWaiting で更新。
- 2026-06-03 **第 4 作「あかり」リンク追加 (`/newgame` 自走)**: companion-games 第 4 作あかり (明度がリソースのデッキ構築ローグライク、シリーズ方向転換作) への導線を `GAMES` 配列に 1 行追加。URL = `https://miho-inspiron-3521.tail5e989b.ts.net:8444/akari/` (同一サーバ・同一ポートで `/akari/` prefix 配信、みちゆき `:8444/`・ともしび `/tomoshibi/`・なごり `/nagori/` は不変)。`renderGames` が `rel=noopener` を一様付与するため個別対応なし。
  - app.js 変更につき **SW CACHE を remote-v9→v10 に bump** (cache-first の旧 shell を更新)。SHELL は新規ファイル無しで変更不要。web 静的なので server 再起動不要・即配信、SW は skipWaiting で次回アクセス時に更新。
- 2026-06-02 **第 3 作「なごり」リンク追加 (`/newgame` 自走)**: companion-games 第 3 作なごり (指でなぞって消えない道を描き、霧の淀みを繋いで自分の一筆を俯瞰する) への導線を `GAMES` 配列に 1 行追加。URL = `https://miho-inspiron-3521.tail5e989b.ts.net:8444/nagori/` (同一サーバ・同一ポートで `/nagori/` prefix 配信、みちゆき `:8444/`・ともしび `/tomoshibi/` は不変)。`renderGames` が `rel=noopener` を一様付与するため個別対応なし。
  - app.js 変更につき **SW CACHE を remote-v8→v9 に bump** (cache-first の旧 shell を更新)。SHELL は新規ファイル無しで変更不要。web 静的なので server 再起動不要・即配信、SW は skipWaiting で次回アクセス時に更新。
- 2026-06-02 **第 2 作「ともしび」リンク追加 (user 質問起点)**: companion-games 第 2 作ともしび (呼びかけると世界が応える歩行ゲーム) への導線を `GAMES` 配列に 1 行追加。URL = `https://miho-inspiron-3521.tail5e989b.ts.net:8444/tomoshibi/` (games は同一サーバ・同一ポートで `/tomoshibi/` prefix 配信、みちゆき `:8444/` は不変)。`renderGames` が `rel=noopener` を一様付与するため個別対応なし。
  - app.js 変更につき **SW CACHE を remote-v7→v8 に bump** (cache-first の旧 shell を更新)。SHELL は新規ファイル無しで変更不要。
  - 検証: `node --check` 構文 OK / code-reviewer 全観点 OK (修正必須なし)。web 静的なので server 再起動不要・即配信、SW は skipWaiting で次回アクセス時に更新。

- 2026-06-02 **ゲームリンク追加 (user 要望)**: companion-games (umbrella「全部 AI で作るゲーム」) のゲームへ、リモコンからアドレスをコピペせず飛べる導線を追加。
  - `#app` 最下部に折りたたみ「ゲーム」カード。リモコン機能(動画/発話/OS status)の邪魔をしないよう **既定で畳む**(タップで一覧展開→各ゲーム名タップで開く、数タップ)。glance トグルと同じ作法(click + Enter/Space keydown, `aria-expanded` + `.open` chevron)。
  - 一覧は `app.js` の `GAMES` 配列駆動(`createElement`/`textContent` のみ、innerHTML 不使用の既存慣習)。第 2 作以降は配列に 1 行追加。現状: みちゆき → `https://miho-inspiron-3521.tail5e989b.ts.net:8444/`(別オリジン = games サーバの別ポート、`rel=noopener` の単純 `<a>` ナビ。CSP `default-src 'self'` はトップレベルナビには効かず fetch もしないので境界に反しない)。games 本番ポート(現 8444)が変わったら `GAMES` の 1 箇所を直す。
  - index.html/app.js/style.css を変更したので **SW CACHE を remote-v6→v7 に bump**(cache-first の旧 shell を更新)。SHELL リストは新規ファイル無しのため変更不要。
  - 検証: Chromium(games の playwright 借用)で 既定畳み→展開で みちゆき リンク href/rel 一致→再畳み、pageerror 0 を PASS。code-reviewer 全観点 OK(修正必須なし)。
  - 関連: ゲーム側に v1 で「トップにバージョン番号表示」要望あり(キャッシュ問題か検証不足かの切り分け用)。games repo 側で対応 → `games/docs/STATUS.md`。

- 2026-05-28 **RV-5 build 検証 🔴必須項目 完了 + 5 秒周期 stall を RV-8 別タスク化**
  - **V-A1 ✅**(再確認、2026-05-28): VOD URL (YouTube 切り抜き 5 分強) を PWA から投入 → TV(HDMI-1)全画面 720p 再生 / `is_live=false` / `seekable=true` / pos 進行 / dashboard BGM mpv (別 socket、9:00 終了後で停止中) と非競合。
  - **V-A3 Live hwdec ✅**(2026-05-28): hololive ENreco Live URL (FUWAWA POV) を投入、`systemctl --user restart` で新 ExecStart(`--hwdec=vaapi --video-zoom=-0.05`) 反映後の同 URL 再投入で IPC `hwdec-current=vaapi` を確認。**estimated-vf-fps 27.3→60.0 (container 60.0 に完全追随) / %CPU 66.7→20.0 (top) / RSS 223→160 MB / decoder-frame-drop=0 維持**。ソフトデコードでは捌けなかった 60fps デコード CPU 負荷をハードに逃がす本筋対応。
  - **V-A6 ✅**(2026-05-28): resident mpv RAM 実測。idle 10.4 MB / 再生中 160-244 MB(3.7Gi の 6.5%)、24/7 常駐妥当性 OK。
  - **video-zoom=-0.05 ✅**(2026-05-28): TV オーバースキャン補正、user 実機で「ちょうどいい」確認済(IPC `set_property video-zoom -0.05` 即時適用 → service file 永続化)。値は 1 回確定、刻まない。
  - **5 秒周期 Live edge stall → RV-8 別タスク化**: hwdec=vaapi 投入後も user TV 実機で 5 秒周期(YouTube Live DASH/HLS segment 長と一致)の固まりが残る。機械測定で `demuxer-cache-state.underrun=True` + `paused-for-cache` 反復 + estimated-vf-fps が 60.0↔37.5 を反復 = ネットワーク受信側の問題と切り分け確定。**対症療法 2 周目ガード抵触リスク高**(`--ytdl-format` 480p 絞り / `--cache-secs` 増量 はいずれも「数値だけを動かす」修正に該当)ゆえ独断で打たず、root cause 切り分け(Wi-Fi 品質 / 経路 / yt-dlp chunk 戦略 / mpv cache 設定)を先行する。TODO RV-8 へ分離、別 session 着手、優先度は Phase 2.6 Telegram 観察 / voice 統合より下。
  - **build 検証 SETUP §6 残🟠(別 TODO 化せず実運用観察に任せる)**: dashboard BGM mpv 同居(別 socket だが PulseAudio sink / 全画面競合は朝 5:30-9:00 で目視)/ 720p deinterlace 1080i 画質(progressive 動画では未検証、必要時に観察)/ 単一 lane 実機再確認(`pgrep -af yt-dlp` 本数)。
  - 設計反映: `~/companion/workspace/redesign/video-design.md` §3.5 実機検証結果(後続 commit) + 改版履歴。

- 2026-05-28 **F-video mpv チューニング(hwdec=vaapi + video-zoom=-0.05)を service に追加**
  - **hwdec=vaapi**: V-A3 Live URL 再生で `hwdec-current=no` のまま frame-drop 690 / estimated-vf-fps 27.3 (container 60.0) を観測。ソフトデコードでは Live 60fps を捌けないことが定量で確定したため有効化。i3-3217U(Ivy Bridge GEN7)+ Intel HD Graphics 4000 は VA-API 対応世代、ホストに `i965-va-driver` インストール済(`/usr/lib/x86_64-linux-gnu/dri/i965_drv_video.so` 存在を確認)。**新規オプション 1 つの追加 = 対症療法 2 周目ガード非該当(1 周目)**。
  - **video-zoom=-0.05**: TV オーバースキャン補正(画面端の見切れ解消)。user 実機で「ちょうどいい」確認済。dashboard BGM mpv(別 unit)には影響しない。値は 1 回確定で刻まない。
  - **検証**: service 編集後 `systemctl --user daemon-reload` → `systemctl --user show companion-video-mpv.service -p ExecStart` で `--hwdec=vaapi` と `--video-zoom=-0.05` 反映を確認。
  - **実 restart は本対応では未実施**: 現プロセス PID 239162 稼働中、user が Live URL 再投入時に frame-drop / hwdec-current の再判定段を lead が握る。
  - **V-A6 ✅**: resident mpv RAM 実測 183-244 MB(3.7Gi の 6.5%、24/7 常駐妥当性 OK)。
  - 設計反映: `~/companion/workspace/redesign/video-design.md` §3.5(hwdec)/ §3.6(video-zoom) + 改版履歴。
- 2026-05-22 **F-video 追加要望 RV-6（URL クリアボタン）+ RV-7（±N スキップ/ステップ可変）実装完了**
  - RV-6: `#video-idle` にクリアボタン。`videoClear` で `#video-url` を空に + `#video-ended`/`#video-play-err` 文言 + 直前 URL ヒント(`V_LASTURL_KEY`)をクリア。last URL も消さないと poll 次 tick の `renderIdle` で文言/再投入が復活するため一緒に落とす。純 client UI。
  - RV-7: 設計メモ採用案 (a)。`/api/video/seek` に `{delta}` 相対モードを追加し `video.seek(amount, relative=True)` → mpv `seek <delta> relative`(verb whitelist「seek abs+rel」内、RCE 面据え置き)。負 delta 許可・`abs<=86400` で範囲制限、範囲外は mpv が clamp（成否1回確定・条件積みの2周目を打たない）。ステップ秒(5/10/30/60s, 既定 10s)は client `localStorage`(`V_STEP_KEY`, token と分離)、サーバは delta を受けるだけ。`seekable=VOD` のみ `#video-skiprow` 表示（live は §5.1 で非表示、`#video-seekrow` と同条件）。
  - 検証: `python3 -m unittest discover -s tests` で 8 テスト pass、`py_compile` OK。code-reviewer 修正必須なし。sw.js CACHE v4→v5 bump。
  - 実機確認(2026-05-22, ユーザー在席)で 3 件の不具合を発覚 → 即修正:
    - ① **クリアボタン行のレイアウト崩れ**: `#video-play` を `.row` 内に入れたのに `margin-top` 規則に残り `.row` の margin と二重がけで縦ずれ → 規則から外し `.row` に一本化(style.css)。
    - ② **再生が前回 pause を継承**(F-video 本体の既存不具合): mpv `pause` は global property で `loadfile` 跨ぎ保持されるため、前回一時停止のまま閉じると次の再生も pause で始まる。`play()` の loadfile に per-file option `pause=no` を付け継承を断つ。実機で再現/復帰確認、mpv 0.34.1 構文確認(video.py)。global pause property は書き換えず pause/resume 経路と独立、成否 1 IPC 確定。
    - ③ **±N スキップ無反応 → 真因はサーバ未再起動**: 当初キャッシュを疑ったが誤り。ステップ秒の上下ボタン(`changeStep`, 純 client)は効くのにスキップ(`videoSkip`, サーバ叩く)だけ効かない、で切り分け確定。`companion-remote.service`(user service)は 2026-05-20 起動のまま稼働し**旧 `app.py` がメモリ常駐**、`{delta}` 分岐が無く 400。静的 `web/` はリクエスト毎ディスク配信ゆえ client だけ新版だった。`systemctl --user restart companion-remote.service` で新コード反映(②pause=no・RV-7 サーバ側もこの再起動で初反映)。sw.js v5→v6 bump は client 新版置換の保険として有効(無害)。
  - **運用知見**: remote の `server/*.py` 編集は `systemctl --user restart companion-remote.service` するまで無反映(Python メモリ常駐)。`web/` 静的ファイルは即反映なので「UI は変わったのに API 挙動が古い」と乖離する。→ §8 churn 手順 / CLAUDE.md 反映候補。
  - 残: 再起動後の実機確認(±N スキップ実動 / レイアウト揃い / 再生が pause で始まらない)。朝 dashboard 稼働(5:30〜9:00)を避ける。
- 2026-05-21 **F-video アーカイブ再生不能を修正（mpv try_ytdl_first=yes）**
  - 症状: 配信終了後アーカイブ（was_live VOD）が PWA から再生できず、解決中表示のあと idle に戻る。
  - 切り分け: yt-dlp `-g` 単体・standalone mpv（フル env）・headless idle+IPC では再生到達。**実 service（systemd 最小 env + 実 vo/HDMI）でのみ ~5秒で idle に中断**（清浄・単一 lane でも再現、競合ではない）。
  - 根本原因: mpv 0.34.1 既定 `ytdl_hook-try_ytdl_first=no` は youtube URL をまず ffmpeg で直開き→必ず失敗→`on_load_fail` で yt-dlp フォールバックする。**実 vo 環境では直開き失敗後・フォールバック完了前に load が idle 中断**され、解決の遅いアーカイブ（yt-dlp 解決 18〜37s と高分散）で顕在化。速い VOD/live はフォールバックが間に合っていた。
  - 修正: service の `--script-opts` に `ytdl_hook-try_ytdl_first=yes` を追加（最初から yt-dlp を呼ぶ＝失敗状態を経由しない、無駄な直開きも消え僅かに高速化）。条件追加・stderr 分岐・リトライ増の 2周目ではなく load 戦略を正す単一オプション。
  - 検証: 実 service mpv にアーカイブ投入 → **t+23s で再生到達**（修正前は t+5s で idle）。standalone でも t+7s 再生確認。
  - 補足: yt-dlp 解決は内蔵 jsinterp で 6/6 成功するが 7〜37s と高分散（ネットワーク律速、node でも改善せず・service PATH に node 無し）。JS ランタイム deprecation 警告は現状ブロックせず、§8 churn の watch 項目として継続。
- 2026-05-20 **F-video build 検証（GUI 実機、V-A1/V-A3 ✅ / V-A6 未了）**
  - **V-A1 ✅**: `--fs-screen=0`(数値 index)はノート本体(LVDS-1=primary)を掴んだ → **出力名指定 `--fs-screen-name=HDMI-1` で TV へ全画面確定**(xrandr: HDMI-1 1920x1080 820x460mm=TV)。idle mpv は `--force-window=no` で窓非占有。実機で TV 全画面 + シーク + 音 OK。脆い数値 index でなく権威ソース(出力名)で決定論化。
  - **V-A3 ✅**: live 検出の当初案「duration=None=live」が **DVR バッファ長を有限 duration で返す**実挙動(46→104→178s と伸長)で外れた。設計 §7 予告通り property を盛らず **`seekable` を主シグナルへ1回引き直し**(`is_live = playing and not seekable`、live=seekable False / VOD=True)。実機で live=●LIVE/シーク無効/pause 有効、VOD=シーク可。
  - **付随バグ修正**: ① `[hidden]` 属性が `.row{display:flex}` に specificity で負け live 時シークバーが残る → CSS `[hidden]{display:none!important}`。② 配信終了/再生不可 URL の clean-fail 時、投入直後の失敗だけ「読み込めませんでした…」と明確化(client 遷移=resolving→idle で playing 未到達、を判定。stateless は崩さない。再開時の取りこぼしは従来の中立文言を維持)。SW CACHE remote-v1→v4。
  - **V-A6 未了**: resident mpv RAM 実測。現状 `systemctl --user start` のみ(`enable` 保留)。RAM 妥当性確認後に boot 自動起動を確定する。
  - 🟠残: dashboard BGM mpv 同居 / 720p deinterlace / 単一 lane 再確認。
  - **アーカイブ視聴**: → 2026-05-21 Done エントリで解決（try_ytdl_first=yes）。配信直後の post_live 処理中は YouTube 側でアーカイブ未公開のため依然 clean-fail（時限、待てば視聴可）。
- 2026-05-20 **F-video（動画プレイヤー）実装完了（RV-1〜RV-5、bot.service 非依存 = v1-α 系列）**
  - 設計 `video-design.md` v1.0 の確定設計どおり実装。Discord `/play`→`xdg-open`→ブラウザ（再生後操作不能）を置き換える、mpv 0.34.1 + yt-dlp ytdl_hook ベースの再生 + pause/seek/音量/停止制御。
  - **RV-1** 常駐 idle mpv unit。**RV-2** urlguard（bot.py ミラー、§4.1 canonical 攻撃ベクタ）+ video.py（IPC verb whitelist 固定テンプレート、PulseAudio sink 不可触、ytdl_hook ゆえ署名付き stream URL を見ない）+ test。**RV-3** `/api/video/*` 7 endpoint。**RV-4** PWA 動画パネル（LIVE モード / RESOLVING UX / close≠stop / glance now-playing / 取りこぼし中立明示）。**RV-5** docs。
  - **スモークテスト（lead、DISPLAY 不要分）**: ① urlguard/video 単体 8 テスト pass ② headless mpv（`--vo=null --ao=null`）で IPC 実プロトコル検証（state 導出 / pause/volume/stop verb 写像 / socket 0o600 確認）③ ハンドラ層 E2E（実 mpv 接続で play allowlist 受理/拒否・seek/volume validation・503/502/400 写像・state 集約を確認）。
  - **未実施（GUI/実 live URL 依存、SETUP §6 = build 検証へ）**: V-A1 `--fs-screen` TV 全画面 + idle 窓非占有 / V-A3 実 live URL 挙動 / V-A6 resident mpv RAM 実測。lead がユーザー在席時に握る（朝 dashboard 稼働中は避ける）。
  - 各サブタスクで code-reviewer レビュー（結果は本エントリ更新時に反映）。
- 2026-05-20 **known-good ペア台帳（yt-dlp churn bound、video-design §8）**
  - **mpv 0.34.1（Mint 21.3 同梱、凍結） + yt-dlp 2026.03.17 + 検証日 2026-05-20 + 結果: 単体/IPC/ハンドラ E2E スモーク pass（実再生は build 検証で確認）**。
  - 破綻シグナル = 全動画が failed。reactive 手動差替（旧版退避→差替→§10 スモーク再走→ロールバック可）。**`yt-dlp -U` 自動更新は禁止**。非互換時は (α)yt-dlp 旧版保留 or (β)mpv 更新/pre-resolve への 1回の設計判断を本欄に記録してから動く（2周目を打たない）。
- 2026-05-20 **bot `/play` の deprecate 判断 = 据え置き（撤去せず）**
  - bot.py 改変は v1-β（6/2 健全性観察窓後）に限定する方針（本 STATUS「位置付け」）と整合。F-video の build 検証（V-A1 TV 全画面実機確認）が pass し実運用に乗るまでは Discord `/play` を残し、両系統併存とする。撤去/リダイレクトの最終判断はユーザー裁定（build 検証 pass 後に F-1 v1-β とまとめて bot.py を触るタイミングで再検討）。
- 2026-05-20 **実機デプロイ + serve 疎通検証**
  - systemd user service 配置・`enable --now`(active、起動時自動起動も登録)。`ss` で 127.0.0.1:47824 のみ listen を確認。
  - `tailscale serve --bg 47824` で公開 → `https://miho-inspiron-3521.tail5e989b.ts.net/`(tailnet only)。**注意**: 初回に「Serve is not enabled on your tailnet」警告が出るが続けて Success し実際に稼働する(curl 実 CA 検証通過)。
  - **§9 検証済**: d-1 ルート `/` マウント(index/manifest/sw/icons 配信)✅ / d-3 ::1 疎通(serve→127.0.0.1)✅ / TLS 実 CA 証明書 ✅ / token 無 `/api/status` 401 ✅ / **d-4 say.sh audio ✅**(voice engine 0.25.2 起動後、remote service 実環境から抽出した最小 env で say.sh exit 0 = 合成+再生 OK。PULSE_SERVER は env に無いが XDG_RUNTIME_DIR/pulse 経由で paplay 到達)。
  - **§9 残**: d-2 identity header(echo endpoint が無く未確認、H-1 は token+ACL で受容済なので必須でない)。
  - PWA は Pixel-6 にインストール済(2026-05-20、user 確認)。OS status / glance 動作確認済。
  - トークン発行は user 自身が `auth.py issue pixel-6` で実施(会話ログに平文を残さないため)。
- 2026-05-20 **v1-α 実装完了（RA-1〜RA-7、各サブタスク 1commit + code-reviewer レビュー）**
  - RA-1 server skeleton(http.server 127.0.0.1:47824, /api/health, ガードレール (i)(ii)(v) 土台)。RA-2 トークン認証(auth.py 発行 CLI, tokens.json 0o600 に SHA-256 保存, Bearer compare_digest, Content-Length cap + chunked 拒否)。RA-3 F-3 OS status(/api/status, df/free/sensors/uptime, ledger 非参照=H-2)。RA-4 F-2 voice(/api/say, say.sh argv 直叩き + env 隔離をスモークで実証, exit code→HTTP, rate-limit)。RA-5 PWA(発話/OS status/token paste/glance, CSP + innerHTML 不使用, SW shell precache + API network-only, icons)。RA-6 systemd service + SETUP.md。RA-7 台帳反映。
  - 各サブタスクで code-reviewer レビュー(修正必須ゼロ、軽微提案は反映 or 台帳記録)。スモークテストは全サブタスク実施(認証/413/chunked/env 隔離/静的配信/traversal 404 等)。
  - **未実施(実機依存、SETUP §9)**: tailscale serve 実機疎通(d-1 ルートマウント / d-2 identity header / d-3 ::1 / d-4 say.sh audio env)。systemd enable + serve 設定は SETUP.md の手順で user 実施。
  - **N-3**: remote-design v1.0 改稿時点で §2.3 が「UI 失効 endpoint は作らない」と正記載済のため、削除対象の stale 記述なし(充足済)。
  - **R1**: PROJECT.md L373 M-14 文面を承認済み改訂案(認可趣旨ベース)へ差替え済。
- 2026-05-20 **(B)GitHub 昇格は見送り、(C) ローカル git のみ継続**(user 判断)。workspace/web と同じ rollback 専用、GitHub remote は意図的に付けない。昇格は後日判断でも可。
- 2026-05-20 **設計議論完了 / remote-design.md v1.0 確定**
  - agent team `companion-remote-design`(architect/ux/security/devil + lead)で Round 1〜3。
  - 致命級 D-1/D-2(案Z+bot.py不可触+F-1会話の三つ巴矛盾)を devil が摘出 → user 差し戻しで対話完結型 + 案B 確定。H-1(認可退行)/H-2(派生原則自己違反)も cross-review で摘出・対処。lead 裁定13件、devil 最終確認 approve 可(致命級ゼロ、残課題 R1〜R4 は実装スコープに畳み済)。
  - 成果物: remote-design.md v1.0 + remote-arch-plan-v{1,2}.md(議論アーカイブ)。
- 2026-05-20 `remote/` をローカル git のみ (remote なし、rollback 専用) で先行 git 化
  - **やったこと**: `git init -b main` + user 設定 + gitleaks pre-commit フック配置 + `.gitignore` 作成 + `gitleaks dir` で no leaks 確認 + 初回 commit。**GitHub remote は意図的に付けない**

## 既知の問題

- **H-1 F-1 認可退行**(設計確定済・**2026-05-20 user 受容済**): F-1 は run_claude 直呼びで on_message の OWNER_ID チェックを通らず、token+ACL(+identity header)の認可で Discord 経路の OWNER_ID 二重より一段弱い。緩和=identity header 二段化 + budget guard backstop(課金月次 cap)+ SSH 失効。F-1 自体は v1-β(6/2 観察窓後)実装なので v1-α には未出現。

## 運用注記

- **Tailscale funnel は絶対に叩かない** (外部公開コマンド、誤発射防止のため `.claude/settings.json` で deny 済)
- サーバは `127.0.0.1` バインドのみ、外向き口は `tailscale serve` 経由だけ
- **web/ アセット(index/app.js/style.css 等)を更新したら `web/sw.js` の `CACHE` を 1 つ上げる**(SW は shell cache-first のため、bump しないと旧版を掴む)
- スマホ紛失時の revoke 手順(2系統): ①Tailscale 管理画面で pixel-6 を device disable(第一手・最上流) ②SSH で `remote/.state/tokens.json` を空に + service restart。**UI 経由のトークン無効化 endpoint は作らない**(漏洩端末から叩ける循環参照のため、N-3 で確定)。
