# Discord bot token (from https://discord.com/developers/applications)
DISCORD_TOKEN=

# Discord user ID allowed to interact with the bot
OWNER_ID=

# Discord text channel ID for socket-routed notifications (Phase 2 reports etc.)
NOTIFY_CHANNEL_ID=

# Absolute path to the `claude` CLI (e.g. ~/.nvm/versions/node/<ver>/bin/claude)
CLAUDE_BIN=

# CWD passed to `claude -p` (Phase 2.5 onward: dedicated bot-workspace)
CLAUDE_CWD=/home/miho/companion/bot-workspace

# Subprocess timeout in seconds
CLAUDE_TIMEOUT=300

# BudgetGuard implementation: requests_count (Phase 2.5 / 〜2026-06-14) or
# credit_usd (T-D 後半, 2026-06-15〜). ENV is the master switch (design.md §4.2).
BOT_BUDGET_GUARD=requests_count

# RequestsCountGuard: rolling 1h request-count cap. default 20 (実弾運用で再調整).
BOT_REQUESTS_PER_HOUR=20
