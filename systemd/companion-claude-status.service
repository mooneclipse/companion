[Unit]
Description=companion-maintenance: poll Claude service status and notify on change
# bot 起動順だけ後ろに寄せる。bot が落ちていても本体スクリプトは
# socket 不在として skip するので Wants=/Requires= は付けない。
After=companion-bot.service

[Service]
Type=oneshot
ExecStart=/home/miho/companion/maintenance/scripts/notify-claude-status.py
