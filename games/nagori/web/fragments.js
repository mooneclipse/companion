"use strict";
// なごり 断章データ。
// これがゲームの核。テキストは一字一句そのまま(発注時に lead が verbatim 確定した静的データ)。
// ランタイムで改変・創作しない。
//
// 構造:
//   OPENING  — 起動時の overlay カード。タップで dismiss(操作導線を兼ねる)。
//   STILLS   — 淀み 7 個に対応する in-canvas 断章。淀みが cleared になった瞬間に
//              その淀み付近へフェードイン → 数秒で消える(タップ不要・読み飛ばし可)。
//              配列順 = 淀みの配置順に対応づける。
//   ENDING   — 全 7 淀み cleared → 俯瞰時に静かに表示。dismiss せず終端。

// opening カード(overlay)。タイトル「なごり」を大きく重ね、操作導線を兼ねる。
const OPENING = {
  title: "なごり",
  text:
    "霧がふかい。\n" +
    "ゆびで、なぞってごらん。\n" +
    "道は、あなたが引いたところにだけ、もういちど生まれる。",
};

// 淀み 7 本(in-canvas)。各淀みが cleared になった瞬間にその淀み付近へフェードイン。
// 順序は淀みの配置順(SILTS の index)に対応づける。
const STILLS = [
  "ここに道があったことを、もう誰もおぼえていない。",
  "あなたがなぞった線が、わたしのいた場所をなぞっている。",
  "曲がるたび、迷ったしるしが残る。それでよかった。",
  "引いてしまった線は、もう消せない。だからきれいだ。",
  "まだ引いていないところは、どこへでも行ける。",
  "だれかも、ここで一度、手を止めた気がする。",
  "霧の向こうにあったのは、なくしたと思っていた道だった。",
];

// ending(overlay)。全 7 淀み cleared → 俯瞰時に静かに表示。dismiss せず終端。
const ENDING = {
  text:
    "ぜんぶ、つながった。\n" +
    "あなたの引いた線が、ひとすじの道になって、\n" +
    "霧のうえに、しずかに残っている。\n" +
    "\n" +
    "これは、あなたが通った跡。\n" +
    "もう、なごりだけ。",
};

// PALETTE: progress = cleared 淀み数 / 7 に沿う 6 キーフレーム(達成軸に色を紐付ける)。
// fog   = 霧/背景の乳白〜藍。明背景から暗背景へ。
// land  = 霧が晴れた下に立ち上がる地形の基調色。
// silt  = 淀み(澱)の染み色(霧より一段暗い)。cleared 後は地形へ寄る。
// line  = 引いた線の基調(視認性ロジックで毎フレーム明度反転調整する基準色)。
// 最終色は前景視認性のコントラスト確保を優先して基調から微調整済み。
const PALETTE = [
  { at: 0.0, fog: "#e8e6df", land: "#cfd2cc", silt: "#b9bbb4", line: "#b86a2a" }, // 乳白(無彩・明背景)
  { at: 0.2, fog: "#d8d4c8", land: "#9fb0b8", silt: "#a7a89f", line: "#c0762e" }, // 薄青が差す
  { at: 0.4, fog: "#b9b3a4", land: "#7d8a5e", silt: "#8d8a7a", line: "#cf8232" }, // 苔緑・砂
  { at: 0.6, fog: "#b0967e", land: "#9a7d56", silt: "#7d6a58", line: "#d98a3a" }, // 夕の気配
  { at: 0.8, fog: "#9c7350", land: "#7a6a72", silt: "#5e5260", line: "#e6b25a" }, // 琥珀に藍
  { at: 1.0, fog: "#4a4a63", land: "#3a3a52", silt: "#34344a", line: "#f0c878" }, // 夕映え→藍(全線が金の筋)
];
